package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_086 implements Buff, SkillBuff, CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public static int[] DURATION = {
        0, -1
    };
    public static String[] LIMIT_SKILLS = {
        "", "24"
    };
    public static int[] CHANGE_B_PHYSICAL_AP_VALUE = {
        0, 0
    };
    public static float[] CHANGE_B_PHYSICAL_AP_PERCENT = {
        0.0f, 10.0f
    };
    Skill skill;
    int instanceID;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    IntHashSet limitSkills = new IntHashSet();
    String limit_skills;
    int change_b_physical_ap_value;
    float change_b_physical_ap_percent;

    public AutoGeneratedBuff_086(Skill skl, int lvl) {
        int dmg = 0;
        skill = skl;
        instanceID = BuffUtil.getNextID();
        level = lvl;
        multiple = 1;
        limit_skills = LIMIT_SKILLS[level];
        change_b_physical_ap_value = CHANGE_B_PHYSICAL_AP_VALUE[level];
        change_b_physical_ap_percent = CHANGE_B_PHYSICAL_AP_PERCENT[level];
        String[] secs = limit_skills.split(",");
        for (String s : secs) {
            limitSkills.add(Integer.parseInt(s));
        }
    }

    public int getId() {
        return skill.getId();
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "射月";
    }

    public String getDesc() {
        return "使用追星逐月技能时伤害提高10%";
    }

    public Skill getSkill() {
        return skill;
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return -1;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return -1;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_086) {
            AutoGeneratedBuff_086 other = (AutoGeneratedBuff_086)buff;
            if (level <= other.level) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure = other.remainCure;
                remainDamage = other.remainDamage;
                remainMPCure = other.remainMPCure;
                remainMPDamage = other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                effectTimes = 0;
                owner = other.owner;
                source = other.source;
                limit_skills = other.limit_skills;
                change_b_physical_ap_value = other.change_b_physical_ap_value;
                change_b_physical_ap_percent = other.change_b_physical_ap_percent;
            }
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
        change_b_physical_ap_value = CHANGE_B_PHYSICAL_AP_VALUE[level];
        change_b_physical_ap_value = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 86, "change_b_physical_ap_value", change_b_physical_ap_value);
        change_b_physical_ap_percent = CHANGE_B_PHYSICAL_AP_PERCENT[level];
        change_b_physical_ap_percent = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 86, "change_b_physical_ap_percent", change_b_physical_ap_percent);
    }

    public void preHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (!limitSkills.contains(context.skill.getGroupId())) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (!limitSkills.contains(context.skill.getGroupId())) {
            return;
        }
    }

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (!limitSkills.contains(context.skill.getGroupId())) {
            return;
        }
        if (isActive && context.damageType == CombatContext.DAMAGE_PHYSICAL) {
            context.attackPower += change_b_physical_ap_value;
            context.attackPowerRate += change_b_physical_ap_percent / 100.0f;
            effectTimes++;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (!limitSkills.contains(context.skill.getGroupId())) {
            return;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (!limitSkills.contains(context.skill.getGroupId())) {
            return;
        }
    }

}
