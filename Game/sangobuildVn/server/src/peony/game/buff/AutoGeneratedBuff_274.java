package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_274 implements Buff, CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public static int[] DURATION = {
        0, -1, -1, -1, -1, -1
    };
    public static float[] IMMUNE_SLOW_RATE = {
        0.0f, 2.0f, 4.0f, 6.0f, 8.0f, 10.0f
    };
    public static float[] IMMUNE_FEAR_RATE = {
        0.0f, 2.0f, 4.0f, 6.0f, 8.0f, 10.0f
    };
    public static float[] IMMUNE_DUMB_RATE = {
        0.0f, 2.0f, 4.0f, 6.0f, 8.0f, 10.0f
    };
    public static float[] IMMUNE_PARALYZE_RATE = {
        0.0f, 2.0f, 4.0f, 6.0f, 8.0f, 10.0f
    };
    public static float[] IMMUNE_STAY_RATE = {
        0.0f, 2.0f, 4.0f, 6.0f, 8.0f, 10.0f
    };
    int instanceID;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    float immune_slow_rate;
    float immune_fear_rate;
    float immune_dumb_rate;
    float immune_paralyze_rate;
    float immune_stay_rate;

    public AutoGeneratedBuff_274(int lvl) {
        int dmg = 0;
        instanceID = BuffUtil.getNextID();
        level = lvl;
        multiple = 1;
        immune_slow_rate = IMMUNE_SLOW_RATE[level];
        immune_fear_rate = IMMUNE_FEAR_RATE[level];
        immune_dumb_rate = IMMUNE_DUMB_RATE[level];
        immune_paralyze_rate = IMMUNE_PARALYZE_RATE[level];
        immune_stay_rate = IMMUNE_STAY_RATE[level];
    }

    public int getId() {
        return 274;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "免疫异常状态";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("免疫所有异常状态（恐惧，麻痹，定身，沉默，减速）的几率提高{0}", CommonUtil.formatPercent(immune_slow_rate * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return -1;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return -1;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public boolean isNeedMerge() {
        return false;
    }

    public boolean merge(Buff buff) {
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
        immune_slow_rate = IMMUNE_SLOW_RATE[level];
        immune_slow_rate = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 274, "immune_slow_rate", immune_slow_rate);
        immune_fear_rate = IMMUNE_FEAR_RATE[level];
        immune_fear_rate = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 274, "immune_fear_rate", immune_fear_rate);
        immune_dumb_rate = IMMUNE_DUMB_RATE[level];
        immune_dumb_rate = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 274, "immune_dumb_rate", immune_dumb_rate);
        immune_paralyze_rate = IMMUNE_PARALYZE_RATE[level];
        immune_paralyze_rate = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 274, "immune_paralyze_rate", immune_paralyze_rate);
        immune_stay_rate = IMMUNE_STAY_RATE[level];
        immune_stay_rate = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 274, "immune_stay_rate", immune_stay_rate);
    }

    public void preHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (!isActive && context.hited() && context.skill instanceof SlowSkill) {
            int rate = (int)(multiple * 100 * immune_slow_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                context.attackResult = CombatContext.ATTACKRESULT_IMMUNE;
                effectTimes++;
            }
        }
        if (!isActive && context.hited() && context.skill instanceof FearSkill) {
            int rate = (int)(multiple * 100 * immune_fear_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                context.attackResult = CombatContext.ATTACKRESULT_IMMUNE;
                effectTimes++;
            }
        }
        if (!isActive && context.hited() && context.skill instanceof DumbSkill) {
            int rate = (int)(multiple * 100 * immune_dumb_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                context.attackResult = CombatContext.ATTACKRESULT_IMMUNE;
                effectTimes++;
            }
        }
        if (!isActive && context.hited() && context.skill instanceof ParalyzeSkill) {
            int rate = (int)(multiple * 100 * immune_paralyze_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                context.attackResult = CombatContext.ATTACKRESULT_IMMUNE;
                effectTimes++;
            }
        }
        if (!isActive && context.hited() && context.skill instanceof StaySkill) {
            int rate = (int)(multiple * 100 * immune_stay_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                context.attackResult = CombatContext.ATTACKRESULT_IMMUNE;
                effectTimes++;
            }
        }
    }

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

}
