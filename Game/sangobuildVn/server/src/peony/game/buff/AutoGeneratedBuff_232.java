package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_232 implements Buff, CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public static int[] DURATION = {
        0, -1
    };
    public static float[] ADD_BUFF_HITED_RATE = {
        0.0f, 10.0f
    };
    public static String[] ADD_BUFF_HITED_RATE_VAR = {
        "", ""
    };
    public static int[] ADD_BUFF_HITED_ID = {
        0, 231
    };
    public static int[] ADD_BUFF_HITED_LEVEL = {
        0, 1
    };
    int instanceID;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    float add_buff_hited_rate;
    String add_buff_hited_rate_var;
    int add_buff_hited_id;
    int add_buff_hited_level;

    public AutoGeneratedBuff_232(int lvl) {
        int dmg = 0;
        instanceID = BuffUtil.getNextID();
        level = lvl;
        multiple = 1;
        add_buff_hited_rate = ADD_BUFF_HITED_RATE[level];
        add_buff_hited_rate_var = ADD_BUFF_HITED_RATE_VAR[level];
        add_buff_hited_id = ADD_BUFF_HITED_ID[level];
        add_buff_hited_level = ADD_BUFF_HITED_LEVEL[level];
    }

    public int getId() {
        return 232;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "被命中后提升生命上限";
    }

    public String getDesc() {
        return "被命中后有几率提升自身生命值上限15%，持续5秒";
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return -1;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return -1;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public boolean isNeedMerge() {
        return false;
    }

    public boolean merge(Buff buff) {
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
        add_buff_hited_rate = ADD_BUFF_HITED_RATE[level];
        add_buff_hited_rate = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 232, "add_buff_hited_rate", add_buff_hited_rate);
        add_buff_hited_level = ADD_BUFF_HITED_LEVEL[level];
        add_buff_hited_level = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 232, "add_buff_hited_level", add_buff_hited_level);
    }

    public void preHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (!isActive && context.hited() && context.isAttack()) {
            boolean hit = false;
            if (add_buff_hited_rate_var.length() == 0) {
                hit = CommonUtil.hit(RND, (int)(multiple * 100 * add_buff_hited_rate), 10000);
            } else {
                Float vo = context.skillParams.get(add_buff_hited_rate_var);
                if (vo != null) {
                    hit = CommonUtil.hit(RND, (int)(multiple * 100 * (vo.floatValue() + add_buff_hited_rate)), 10000);
                }
            }
            if (hit) {
                context.target.buffs.addBuff(BuffUtil.createBuff(add_buff_hited_id, add_buff_hited_level, context.target, context.target, context.damage));
                effectTimes++;
            }
        }
    }

}
