package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_329 implements Buff, SkillEnhancer, Updatable {
    private static final int[] WEAPON = {
        
    };
    public static int[] DURATION = {
        0, 900000, 900000, 900000, 900000, 900000
    };
    public static String[] CHANGE_CD_SKILLS = {
        "", "67", "67", "67", "67", "67"
    };
    public static float[] CHANGE_CD_PERCENT = {
        0.0f, -12.0f, -24.0f, -36.0f, -48.0f, -60.0f
    };
    public static String[] CHANGE_ACTTIME_SKILLS = {
        "", "67", "67", "67", "67", "67"
    };
    public static float[] CHANGE_ACTTIME_PERCENT = {
        0.0f, -100.0f, -100.0f, -100.0f, -100.0f, -100.0f
    };
    int instanceID;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    String change_cd_skills;
    float change_cd_percent;
    String change_acttime_skills;
    float change_acttime_percent;

    public AutoGeneratedBuff_329(int lvl, Unit src, Unit tgt, int dmg) {
        if (src != null) {
            source = src.ref();
        }
        if (tgt != null) {
            owner = tgt.ref();
        }
        instanceID = BuffUtil.getNextID();
        level = lvl;
        endTime = Time.currTime + BuffUtil.enhanceParam(src, tgt, 329, "buffTime", DURATION[level]);
        multiple = 1;
        change_cd_skills = CHANGE_CD_SKILLS[level];
        change_cd_percent = CHANGE_CD_PERCENT[level];
        change_cd_percent = BuffUtil.enhanceParam(src, tgt, 329, "change_cd_percent", change_cd_percent);
        change_acttime_skills = CHANGE_ACTTIME_SKILLS[level];
        change_acttime_percent = CHANGE_ACTTIME_PERCENT[level];
        change_acttime_percent = BuffUtil.enhanceParam(src, tgt, 329, "change_acttime_percent", change_acttime_percent);
    }

    public int getId() {
        return 329;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "强化返魂秘法";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("施放返魂秘法时，施法时间降低为零，同时返魂秘法冷却时间降低{0}", CommonUtil.formatPercent(change_cd_percent * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return endTime;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return true;
    }

    public int getIconID() {
        return 37 | 0x1000000;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_329) {
            AutoGeneratedBuff_329 other = (AutoGeneratedBuff_329)buff;
            if (level <= other.level) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure = other.remainCure;
                remainDamage = other.remainDamage;
                remainMPCure = other.remainMPCure;
                remainMPDamage = other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                effectTimes = 0;
                owner = other.owner;
                source = other.source;
                change_cd_skills = other.change_cd_skills;
                change_cd_percent = other.change_cd_percent;
                change_acttime_skills = other.change_acttime_skills;
                change_acttime_percent = other.change_acttime_percent;
            }
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
    }

    public boolean update(int diff) {
        if (endTime <= Time.currTime) {
            return true;
        }
        return false;
    }

    public void load(byte[] bytes) {
        ByteBuffer buf = ByteBuffer.wrap(bytes);
        owner = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        source = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        level = buf.getInt();
        endTime = Time.elapseTime(buf.getLong());
        multiple = buf.getInt();
        change_cd_skills = CHANGE_CD_SKILLS[level];
        change_cd_percent = CHANGE_CD_PERCENT[level];
        change_acttime_skills = CHANGE_ACTTIME_SKILLS[level];
        change_acttime_percent = CHANGE_ACTTIME_PERCENT[level];
        remainSeconds = buf.getInt();
        tickInterval = buf.getInt();
        remainCure = buf.getInt();
        remainDamage = buf.getInt();
        remainMPCure = buf.getInt();
        remainMPDamage = buf.getInt();
        remainAbsorb = buf.getInt();
        effectTimes = buf.getInt();
    }

    public byte[] save() {
        ByteBuffer buf = ByteBuffer.allocate(66, false);
        buf.put(owner.type);
        buf.putInt(owner.id);
        buf.putInt(owner.instanceId);
        buf.put(source.type);
        buf.putInt(source.id);
        buf.putInt(source.instanceId);
        buf.putInt(level);
        buf.putLong(Time.currentTimeMillis(endTime));
        buf.putInt(multiple);
        buf.putInt(remainSeconds);
        buf.putInt(tickInterval);
        buf.putInt(remainCure);
        buf.putInt(remainDamage);
        buf.putInt(remainMPCure);
        buf.putInt(remainMPDamage);
        buf.putInt(remainAbsorb);
        buf.putInt(effectTimes);
        return buf.array();
    }

    public boolean update2(int time) {
        if (endTime <= Time.currTime) {
            return false;
        }
        return true;
    }

    private IntHashSet changeSkills;
    public IntHashSet getAffectSkillIDs() {
        if (changeSkills == null) {
            int[] idarr;
            changeSkills = new IntHashSet();
            idarr = Utils.stringToIntArray(change_cd_skills, ',');
            for (int sid : idarr) {
                changeSkills.add(sid);
            }
            idarr = Utils.stringToIntArray(change_acttime_skills, ',');
            for (int sid : idarr) {
                changeSkills.add(sid);
            }
        }
        return changeSkills;
    }

    public float updateCDTime(Skill skill, float cd) {
            return cd * (1.0f + change_cd_percent / 100.0f);
    }

    public float updateDistance(Skill skill, float distance) {
            return distance;
    }

    public float updateActTime(Skill skill, float actTime) {
            return actTime * (1.0f + change_acttime_percent / 100.0f);
    }

    public float updateRange(Skill skill, float range) {
            return range;
    }

    public float updateMP(Skill skill, float mp) {
            return mp;
    }

}
