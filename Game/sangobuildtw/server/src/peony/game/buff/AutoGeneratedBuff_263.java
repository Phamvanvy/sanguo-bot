package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;

public class AutoGeneratedBuff_263 implements Buff, PropertyEnhancer, Updatable, CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public static int[] DURATION = {
        0, 5000, 5000, 5000, 5000, 5000
    };
    public static int[] IGNORE_ARMOR_VALUE = {
        0, 0, 0, 0, 0, 0
    };
    public static float[] IGNORE_ARMOR_PERCENT = {
        0.0f, 20.0f, 25.0f, 30.0f, 35.0f, 40.0f
    };
    public static int[] IGNORE_MAGIC_ARMOR_VALUE = {
        0, 0, 0, 0, 0, 0
    };
    public static float[] IGNORE_MAGIC_ARMOR_PERCENT = {
        0.0f, 20.0f, 25.0f, 30.0f, 35.0f, 40.0f
    };
    public static int[] CHANGE_ARMOR_VALUE = {
        0, 0, 0, 0, 0, 0
    };
    public static float[] CHANGE_ARMOR_PERCENT = {
        0.0f, -15.0f, -20.0f, -25.0f, -30.0f, -35.0f
    };
    public static int[] CHANGE_MAGIC_ARMOR_VALUE = {
        0, 0, 0, 0, 0, 0
    };
    public static float[] CHANGE_MAGIC_ARMOR_PERCENT = {
        0.0f, -15.0f, -20.0f, -25.0f, -30.0f, -35.0f
    };
    int instanceID;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    int ignore_armor_value;
    float ignore_armor_percent;
    int ignore_magic_armor_value;
    float ignore_magic_armor_percent;
    int change_armor_value;
    float change_armor_percent;
    int change_magic_armor_value;
    float change_magic_armor_percent;

    public AutoGeneratedBuff_263(int lvl, Unit src, Unit tgt, int dmg) {
        if (src != null) {
            source = src.ref();
        }
        if (tgt != null) {
            owner = tgt.ref();
        }
        instanceID = BuffUtil.getNextID();
        level = lvl;
        endTime = Time.currTime + BuffUtil.enhanceParam(src, tgt, 263, "buffTime", DURATION[level]);
        multiple = 1;
        ignore_armor_value = IGNORE_ARMOR_VALUE[level];
        ignore_armor_value = BuffUtil.enhanceParam(src, tgt, 263, "ignore_armor_value", ignore_armor_value);
        ignore_armor_percent = IGNORE_ARMOR_PERCENT[level];
        ignore_armor_percent = BuffUtil.enhanceParam(src, tgt, 263, "ignore_armor_percent", ignore_armor_percent);
        ignore_magic_armor_value = IGNORE_MAGIC_ARMOR_VALUE[level];
        ignore_magic_armor_value = BuffUtil.enhanceParam(src, tgt, 263, "ignore_magic_armor_value", ignore_magic_armor_value);
        ignore_magic_armor_percent = IGNORE_MAGIC_ARMOR_PERCENT[level];
        ignore_magic_armor_percent = BuffUtil.enhanceParam(src, tgt, 263, "ignore_magic_armor_percent", ignore_magic_armor_percent);
        change_armor_value = CHANGE_ARMOR_VALUE[level];
        change_armor_value = BuffUtil.enhanceParam(src, tgt, 263, "change_armor_value", change_armor_value);
        change_armor_percent = CHANGE_ARMOR_PERCENT[level];
        change_armor_percent = BuffUtil.enhanceParam(src, tgt, 263, "change_armor_percent", change_armor_percent);
        change_magic_armor_value = CHANGE_MAGIC_ARMOR_VALUE[level];
        change_magic_armor_value = BuffUtil.enhanceParam(src, tgt, 263, "change_magic_armor_value", change_magic_armor_value);
        change_magic_armor_percent = CHANGE_MAGIC_ARMOR_PERCENT[level];
        change_magic_armor_percent = BuffUtil.enhanceParam(src, tgt, 263, "change_magic_armor_percent", change_magic_armor_percent);
    }

    public int getId() {
        return 263;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "勇往直前";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("攻擊無視對方護甲法防{0},同時自身護甲法防降低{1}", CommonUtil.formatPercent(ignore_armor_percent * multiple), CommonUtil.formatPercent(change_armor_percent * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return endTime;
    }

    public boolean isGood() {
        return false;
    }

    public boolean dispelable() {
        return true;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return 19 | 0x1000000;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_263) {
            AutoGeneratedBuff_263 other = (AutoGeneratedBuff_263)buff;
            if (source.equals(other.source)) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure += other.remainCure;
                remainDamage += other.remainDamage;
                remainMPCure += other.remainMPCure;
                remainMPDamage += other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                ignore_armor_value = other.ignore_armor_value;
                ignore_armor_percent = other.ignore_armor_percent;
                ignore_magic_armor_value = other.ignore_magic_armor_value;
                ignore_magic_armor_percent = other.ignore_magic_armor_percent;
                change_armor_value = other.change_armor_value;
                change_armor_percent = other.change_armor_percent;
                change_magic_armor_value = other.change_magic_armor_value;
                change_magic_armor_percent = other.change_magic_armor_percent;
                return true;
            }
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
    }

    public boolean update(int diff) {
        if (endTime <= Time.currTime) {
            return true;
        }
        return false;
    }

    public void load(byte[] bytes) {
        ByteBuffer buf = ByteBuffer.wrap(bytes);
        owner = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        source = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        level = buf.getInt();
        endTime = Time.elapseTime(buf.getLong());
        multiple = buf.getInt();
        ignore_armor_value = IGNORE_ARMOR_VALUE[level];
        ignore_armor_percent = IGNORE_ARMOR_PERCENT[level];
        ignore_magic_armor_value = IGNORE_MAGIC_ARMOR_VALUE[level];
        ignore_magic_armor_percent = IGNORE_MAGIC_ARMOR_PERCENT[level];
        change_armor_value = CHANGE_ARMOR_VALUE[level];
        change_armor_percent = CHANGE_ARMOR_PERCENT[level];
        change_magic_armor_value = CHANGE_MAGIC_ARMOR_VALUE[level];
        change_magic_armor_percent = CHANGE_MAGIC_ARMOR_PERCENT[level];
        remainSeconds = buf.getInt();
        tickInterval = buf.getInt();
        remainCure = buf.getInt();
        remainDamage = buf.getInt();
        remainMPCure = buf.getInt();
        remainMPDamage = buf.getInt();
        remainAbsorb = buf.getInt();
        effectTimes = buf.getInt();
    }

    public byte[] save() {
        ByteBuffer buf = ByteBuffer.allocate(66, false);
        buf.put(owner.type);
        buf.putInt(owner.id);
        buf.putInt(owner.instanceId);
        buf.put(source.type);
        buf.putInt(source.id);
        buf.putInt(source.instanceId);
        buf.putInt(level);
        buf.putLong(Time.currentTimeMillis(endTime));
        buf.putInt(multiple);
        buf.putInt(remainSeconds);
        buf.putInt(tickInterval);
        buf.putInt(remainCure);
        buf.putInt(remainDamage);
        buf.putInt(remainMPCure);
        buf.putInt(remainMPDamage);
        buf.putInt(remainAbsorb);
        buf.putInt(effectTimes);
        return buf.array();
    }

    public boolean update2(int time) {
        endTime += time;
        if (endTime <= Time.currTime) {
            return false;
        }
        return true;
    }

    public void enhance(PropertyCalculator pc) {
        if (!checkWeapon(pc.unit)) {
            return;
        }
        pc.defenseRate += multiple * change_armor_percent / 100.0f;
        pc.defense += multiple * change_armor_value;
        if (pc.defense < 0) {
            pc.defense = 0;
        }
        pc.spellDefenseRate += multiple * change_magic_armor_percent / 100.0f;
        pc.spelldefense += multiple * change_magic_armor_value;
        if (pc.spelldefense < 0) {
            pc.spelldefense = 0;
        }
    }

    public void preHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (isActive && context.damageType == CombatContext.DAMAGE_PHYSICAL) {
            context.armor -= multiple * ignore_armor_value;
            context.armorRate -= multiple * ignore_armor_percent / 100.0f;
            effectTimes++;
        }
        if (isActive && context.damageType == CombatContext.DAMAGE_MAGIC) {
            context.armor -= multiple * ignore_magic_armor_value;
            context.armorRate -= multiple * ignore_magic_armor_percent / 100.0f;
            effectTimes++;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

}
