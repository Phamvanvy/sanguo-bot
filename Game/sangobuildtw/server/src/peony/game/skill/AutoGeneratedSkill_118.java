package peony.game.skill;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;

public class AutoGeneratedSkill_118 extends AbstractSkill implements CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public static int[] REQUIRE_LEVEL = {
        0, 0
    };
    public static float[] MP_USE = {
        0.0f, 0.0f
    };
    public static int[] ACT_TIME = {
        0, 0
    };
    public static int[] CD_TIME = {
        0, 0
    };
    public static int[] DISTANCE = {
        0, 40
    };
    public static int[] RANGE = {
        0, 80
    };
    public static float[] ADD_HP_RATE = {
        0.0f, 100.0f
    };
    public static int[] ADD_HP_VALUE = {
        0, 0
    };
    public static float[] ADD_HP_PERCENT_OF_MAX = {
        0.0f, 0.0f
    };
    public static float[] ADD_HP_PERCENT_OF_DAMAGE = {
        0.0f, 40.0f
    };
    float add_hp_rate;
    int add_hp_value;
    float add_hp_percent_of_max;
    float add_hp_percent_of_damage;

    public AutoGeneratedSkill_118(int lvl) {
        super(118, "群體吸血", lvl);
        if (lvl > 1) {
            throw new IllegalArgumentException();
        }
        add_hp_rate = ADD_HP_RATE[lvl];
        add_hp_value = ADD_HP_VALUE[lvl];
        add_hp_percent_of_max = ADD_HP_PERCENT_OF_MAX[lvl];
        add_hp_percent_of_damage = ADD_HP_PERCENT_OF_DAMAGE[lvl];
        distance = DISTANCE[lvl];
        actTime = ACT_TIME[lvl];
        CDGroup = 118;
        CDTime = CD_TIME[lvl];
        range = RANGE[lvl];
        iconId = -1;
        prepareAnimation = -1;
        castAnimation = -1;
        hitAnimation = 116;
        clazz = -1;
        mp = MP_USE[lvl];
        type = TYPE_ATTACK;
        type |= TYPE_VISIBLE;
        type |= TYPE_RIDE_USE;
        targetType = TARGET_AOE_ATTACK_TARGET;
    }

    public String getDesc(Unit owner) {
        return "吸取10尺範圍內所有目標的生命值,吸取量為對其造成傷害20%";
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getRequireLevel() {
        return REQUIRE_LEVEL[level];
    }

    protected CombatEffect createActEffect() {
        if (level == 0) {
            return null;
        }
        return this;
    }

    public Buff newBuff() {
        return null;
    }

    public Buff getAreaBuff() {
        return null;
    }

    public void preHit(CombatContext context, boolean isActive) {
        context.damageType = CombatContext.DAMAGE_MAGIC;
        if (!checkWeapon(context.source)) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {}

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(context.source)) {
            return;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(context.source)) {
            return;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(context.source)) {
            return;
        }
        if (context.hited() && context.isDamage()) {
            int rate = (int)(100 * context.getSkillParam(this, "add_hp_rate", add_hp_rate));
            if (CommonUtil.hit(RND, rate, 10000)) {
                int cure = context.getSkillParam(this, "add_hp_value", add_hp_value);
                cure += context.source.maxhp * context.getSkillParam(this, "add_hp_percent_of_max", add_hp_percent_of_max) / 100.0f;
                cure += context.damage * context.getSkillParam(this, "add_hp_percent_of_damage", add_hp_percent_of_damage) / 100.0f;
                context.activeSkills.add(new FixedHealSkill(cure));
            }
        }
    }

}
