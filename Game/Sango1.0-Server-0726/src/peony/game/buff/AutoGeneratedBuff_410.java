package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_410 implements Buff, CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, -1
    };
    public static float[] DEC_MP_RATE = {
        0.0f, 15.0f
    };
    public static int[] DEC_MP_VALUE = {
        0, 0
    };
    public static float[] DEC_MP_PERCENT_OF_MAX = {
        0.0f, 0.0f
    };
    public static float[] DEC_MP_PERCENT_OF_DAMAGE = {
        0.0f, 30.0f
    };
    public static float[] REPEAT_RATE = {
        0.0f, 20.0f
    };
    public static float[] ADD_HP_RATE = {
        0.0f, 15.0f
    };
    public static int[] ADD_HP_VALUE = {
        0, 0
    };
    public static float[] ADD_HP_PERCENT_OF_MAX = {
        0.0f, 0.0f
    };
    public static float[] ADD_HP_PERCENT_OF_DAMAGE = {
        0.0f, 30.0f
    };
    int instanceID;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    float dec_mp_rate;
    int dec_mp_value;
    float dec_mp_percent_of_max;
    float dec_mp_percent_of_damage;
    float repeat_rate;
    float add_hp_rate;
    int add_hp_value;
    float add_hp_percent_of_max;
    float add_hp_percent_of_damage;

    public AutoGeneratedBuff_410(int lvl) {
        int dmg = 0;
        instanceID = BuffUtil.getNextID();
        level = lvl;
        multiple = 1;
        dec_mp_rate = DEC_MP_RATE[level];
        dec_mp_value = DEC_MP_VALUE[level];
        dec_mp_percent_of_max = DEC_MP_PERCENT_OF_MAX[level];
        dec_mp_percent_of_damage = DEC_MP_PERCENT_OF_DAMAGE[level];
        repeat_rate = REPEAT_RATE[level];
        add_hp_rate = ADD_HP_RATE[level];
        add_hp_value = ADD_HP_VALUE[level];
        add_hp_percent_of_max = ADD_HP_PERCENT_OF_MAX[level];
        add_hp_percent_of_damage = ADD_HP_PERCENT_OF_DAMAGE[level];
    }

    public int getId() {
        return 410;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "国公武器";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("命中目标后有{0}几率燃烧目标伤害量{1}的气力/精力；有{2}几率吸取对目标伤害的{3}作为生命恢复；攻击有{4}几率连击", CommonUtil.formatPercent(dec_mp_rate * multiple), CommonUtil.formatPercent(dec_mp_percent_of_damage * multiple), CommonUtil.formatPercent(add_hp_rate * multiple), CommonUtil.formatPercent(add_hp_percent_of_damage * multiple), CommonUtil.formatPercent(repeat_rate * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return -1;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return -1;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_410) {
            AutoGeneratedBuff_410 other = (AutoGeneratedBuff_410)buff;
            if (level <= other.level) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure = other.remainCure;
                remainDamage = other.remainDamage;
                remainMPCure = other.remainMPCure;
                remainMPDamage = other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                effectTimes = 0;
                owner = other.owner;
                source = other.source;
                dec_mp_rate = other.dec_mp_rate;
                dec_mp_value = other.dec_mp_value;
                dec_mp_percent_of_max = other.dec_mp_percent_of_max;
                dec_mp_percent_of_damage = other.dec_mp_percent_of_damage;
                repeat_rate = other.repeat_rate;
                add_hp_rate = other.add_hp_rate;
                add_hp_value = other.add_hp_value;
                add_hp_percent_of_max = other.add_hp_percent_of_max;
                add_hp_percent_of_damage = other.add_hp_percent_of_damage;
            }
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
        dec_mp_rate = DEC_MP_RATE[level];
        dec_mp_rate = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 410, "dec_mp_rate", dec_mp_rate);
        dec_mp_value = DEC_MP_VALUE[level];
        dec_mp_value = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 410, "dec_mp_value", dec_mp_value);
        dec_mp_percent_of_max = DEC_MP_PERCENT_OF_MAX[level];
        dec_mp_percent_of_max = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 410, "dec_mp_percent_of_max", dec_mp_percent_of_max);
        dec_mp_percent_of_damage = DEC_MP_PERCENT_OF_DAMAGE[level];
        dec_mp_percent_of_damage = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 410, "dec_mp_percent_of_damage", dec_mp_percent_of_damage);
        repeat_rate = REPEAT_RATE[level];
        repeat_rate = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 410, "repeat_rate", repeat_rate);
        add_hp_rate = ADD_HP_RATE[level];
        add_hp_rate = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 410, "add_hp_rate", add_hp_rate);
        add_hp_value = ADD_HP_VALUE[level];
        add_hp_value = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 410, "add_hp_value", add_hp_value);
        add_hp_percent_of_max = ADD_HP_PERCENT_OF_MAX[level];
        add_hp_percent_of_max = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 410, "add_hp_percent_of_max", add_hp_percent_of_max);
        add_hp_percent_of_damage = ADD_HP_PERCENT_OF_DAMAGE[level];
        add_hp_percent_of_damage = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 410, "add_hp_percent_of_damage", add_hp_percent_of_damage);
    }

    public void preHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (isActive && context.hited() && context.isDamage()) {
            int rate = (int)(multiple * 100 * dec_mp_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                int decmp = dec_mp_value;
                decmp += context.target.maxmp * dec_mp_percent_of_max / 100.0f;
                decmp += context.damage * dec_mp_percent_of_damage / 100.0f;
                context.activeSkills.add(new FixedDecMPSkill(decmp));
                effectTimes++;
            }
        }
        if (isActive && context.hited() && context.isDamage()) {
            if (CommonUtil.hit(RND, (int)(multiple * 100 * repeat_rate), 10000)) {
                context.activeSkills.add(context.skill);
                effectTimes++;
            }
        }
        if (isActive && context.hited() && context.isDamage()) {
            int rate = (int)(multiple * 100 * add_hp_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                int cure = add_hp_value;
                cure += context.source.maxhp * add_hp_percent_of_max / 100.0f;
                cure += context.damage * add_hp_percent_of_damage / 100.0f;
                context.activeSkills.add(new FixedHealSkill(cure));
                effectTimes++;
            }
        }
    }

}
