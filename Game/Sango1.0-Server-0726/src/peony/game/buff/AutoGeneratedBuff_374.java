package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_374 implements Buff, CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, -1, -1, -1, -1, -1
    };
    public static float[] ADD_DEBUFF_HITED_RATE = {
        0.0f, 20.0f, 20.0f, 20.0f, 20.0f, 20.0f
    };
    public static String[] ADD_DEBUFF_HITED_RATE_VAR = {
        "", "", "", "", "", ""
    };
    public static int[] ADD_DEBUFF_HITED_ID = {
        0, 375, 375, 375, 375, 375
    };
    public static int[] ADD_DEBUFF_HITED_LEVEL = {
        0, 1, 2, 3, 4, 5
    };
    int instanceID;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    float add_debuff_hited_rate;
    String add_debuff_hited_rate_var;
    int add_debuff_hited_id;
    int add_debuff_hited_level;

    public AutoGeneratedBuff_374(int lvl) {
        int dmg = 0;
        instanceID = BuffUtil.getNextID();
        level = lvl;
        multiple = 1;
        add_debuff_hited_rate = ADD_DEBUFF_HITED_RATE[level];
        add_debuff_hited_rate_var = ADD_DEBUFF_HITED_RATE_VAR[level];
        add_debuff_hited_id = ADD_DEBUFF_HITED_ID[level];
        add_debuff_hited_level = ADD_DEBUFF_HITED_LEVEL[level];
    }

    public int getId() {
        return 374;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "被攻击削弱目标";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("被攻击时，有{0}几率使攻击者攻击命中下降{1}", CommonUtil.formatPercent(add_debuff_hited_rate * multiple), CommonUtil.formatPercent(peony.game.buff.AutoGeneratedBuff_375.CHANGE_B_PHYSICAL_AP_PERCENT[add_debuff_hited_level] * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return -1;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return -1;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_374) {
            AutoGeneratedBuff_374 other = (AutoGeneratedBuff_374)buff;
            if (level <= other.level) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure = other.remainCure;
                remainDamage = other.remainDamage;
                remainMPCure = other.remainMPCure;
                remainMPDamage = other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                effectTimes = 0;
                owner = other.owner;
                source = other.source;
                add_debuff_hited_rate = other.add_debuff_hited_rate;
                add_debuff_hited_rate_var = other.add_debuff_hited_rate_var;
                add_debuff_hited_id = other.add_debuff_hited_id;
                add_debuff_hited_level = other.add_debuff_hited_level;
            }
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
        add_debuff_hited_rate = ADD_DEBUFF_HITED_RATE[level];
        add_debuff_hited_rate = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 374, "add_debuff_hited_rate", add_debuff_hited_rate);
        add_debuff_hited_level = ADD_DEBUFF_HITED_LEVEL[level];
        add_debuff_hited_level = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 374, "add_debuff_hited_level", add_debuff_hited_level);
    }

    public void preHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (!isActive && context.hited() && context.isAttack()) {
            boolean hit = false;
            if (add_debuff_hited_rate_var.length() == 0) {
                hit = CommonUtil.hit(RND, (int)(multiple * 100 * add_debuff_hited_rate), 10000);
            } else {
                Float vo = context.skillParams.get(add_debuff_hited_rate_var);
                if (vo != null) {
                    hit = CommonUtil.hit(RND, (int)(multiple * 100 * (vo.floatValue() + add_debuff_hited_rate)), 10000);
                }
            }
            if (hit) {
                context.source.buffs.addBuff(BuffUtil.createBuff(add_debuff_hited_id, add_debuff_hited_level, context.target, context.source, context.damage));
                effectTimes++;
            }
        }
    }

}
