package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_256 implements Buff, Updatable, CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, 20000
    };
    public static float[] IMMUNE_PHYSICAL_ATTACK_RATE = {
        0.0f, 100.0f
    };
    public static float[] IMMUNE_MAGIC_ATTACK_RATE = {
        0.0f, 100.0f
    };
    public static float[] IMMUNE_SLOW_RATE = {
        0.0f, 100.0f
    };
    public static float[] IMMUNE_FEAR_RATE = {
        0.0f, 100.0f
    };
    public static float[] IMMUNE_DUMB_RATE = {
        0.0f, 100.0f
    };
    public static float[] IMMUNE_PARALYZE_RATE = {
        0.0f, 100.0f
    };
    public static float[] IMMUNE_STAY_RATE = {
        0.0f, 100.0f
    };
    public static float[] IMMUNE_BREAK_RATE = {
        0.0f, 100.0f
    };
    int instanceID;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    float immune_physical_attack_rate;
    float immune_magic_attack_rate;
    float immune_slow_rate;
    float immune_fear_rate;
    float immune_dumb_rate;
    float immune_paralyze_rate;
    float immune_stay_rate;
    float immune_break_rate;

    public AutoGeneratedBuff_256(int lvl, Unit src, Unit tgt, int dmg) {
        if (src != null) {
            source = src.ref();
        }
        if (tgt != null) {
            owner = tgt.ref();
        }
        instanceID = BuffUtil.getNextID();
        level = lvl;
        endTime = Time.currTime + BuffUtil.enhanceParam(src, tgt, 256, "buffTime", DURATION[level]);
        multiple = 1;
        immune_physical_attack_rate = IMMUNE_PHYSICAL_ATTACK_RATE[level];
        immune_physical_attack_rate = BuffUtil.enhanceParam(src, tgt, 256, "immune_physical_attack_rate", immune_physical_attack_rate);
        immune_magic_attack_rate = IMMUNE_MAGIC_ATTACK_RATE[level];
        immune_magic_attack_rate = BuffUtil.enhanceParam(src, tgt, 256, "immune_magic_attack_rate", immune_magic_attack_rate);
        immune_slow_rate = IMMUNE_SLOW_RATE[level];
        immune_slow_rate = BuffUtil.enhanceParam(src, tgt, 256, "immune_slow_rate", immune_slow_rate);
        immune_fear_rate = IMMUNE_FEAR_RATE[level];
        immune_fear_rate = BuffUtil.enhanceParam(src, tgt, 256, "immune_fear_rate", immune_fear_rate);
        immune_dumb_rate = IMMUNE_DUMB_RATE[level];
        immune_dumb_rate = BuffUtil.enhanceParam(src, tgt, 256, "immune_dumb_rate", immune_dumb_rate);
        immune_paralyze_rate = IMMUNE_PARALYZE_RATE[level];
        immune_paralyze_rate = BuffUtil.enhanceParam(src, tgt, 256, "immune_paralyze_rate", immune_paralyze_rate);
        immune_stay_rate = IMMUNE_STAY_RATE[level];
        immune_stay_rate = BuffUtil.enhanceParam(src, tgt, 256, "immune_stay_rate", immune_stay_rate);
        immune_break_rate = IMMUNE_BREAK_RATE[level];
        immune_break_rate = BuffUtil.enhanceParam(src, tgt, 256, "immune_break_rate", immune_break_rate);
    }

    public int getId() {
        return 256;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "20√ÎŒﬁµ–";
    }

    public String getDesc() {
        return "20√ÎŒﬁµ–";
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return endTime;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return 119 | 0x1000000;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public boolean isNeedMerge() {
        return false;
    }

    public boolean merge(Buff buff) {
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
    }

    public boolean update(int diff) {
        if (endTime <= Time.currTime) {
            return true;
        }
        return false;
    }

    public void load(byte[] bytes) {
        ByteBuffer buf = ByteBuffer.wrap(bytes);
        owner = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        source = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        level = buf.getInt();
        endTime = Time.elapseTime(buf.getLong());
        multiple = buf.getInt();
        immune_physical_attack_rate = IMMUNE_PHYSICAL_ATTACK_RATE[level];
        immune_magic_attack_rate = IMMUNE_MAGIC_ATTACK_RATE[level];
        immune_slow_rate = IMMUNE_SLOW_RATE[level];
        immune_fear_rate = IMMUNE_FEAR_RATE[level];
        immune_dumb_rate = IMMUNE_DUMB_RATE[level];
        immune_paralyze_rate = IMMUNE_PARALYZE_RATE[level];
        immune_stay_rate = IMMUNE_STAY_RATE[level];
        immune_break_rate = IMMUNE_BREAK_RATE[level];
        remainSeconds = buf.getInt();
        tickInterval = buf.getInt();
        remainCure = buf.getInt();
        remainDamage = buf.getInt();
        remainMPCure = buf.getInt();
        remainMPDamage = buf.getInt();
        remainAbsorb = buf.getInt();
        effectTimes = buf.getInt();
    }

    public byte[] save() {
        ByteBuffer buf = ByteBuffer.allocate(66, false);
        buf.put(owner.type);
        buf.putInt(owner.id);
        buf.putInt(owner.instanceId);
        buf.put(source.type);
        buf.putInt(source.id);
        buf.putInt(source.instanceId);
        buf.putInt(level);
        buf.putLong(Time.currentTimeMillis(endTime));
        buf.putInt(multiple);
        buf.putInt(remainSeconds);
        buf.putInt(tickInterval);
        buf.putInt(remainCure);
        buf.putInt(remainDamage);
        buf.putInt(remainMPCure);
        buf.putInt(remainMPDamage);
        buf.putInt(remainAbsorb);
        buf.putInt(effectTimes);
        return buf.array();
    }

    public boolean update2(int time) {
        endTime += time;
        if (endTime <= Time.currTime) {
            return false;
        }
        return true;
    }

    public void preHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (!isActive && context.hited() && context.damageType == CombatContext.DAMAGE_PHYSICAL) {
            int rate = (int)(multiple * 100 * immune_physical_attack_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                context.attackResult = CombatContext.ATTACKRESULT_IMMUNE;
                effectTimes++;
            }
        }
        if (!isActive && context.hited() && context.damageType == CombatContext.DAMAGE_MAGIC) {
            int rate = (int)(multiple * 100 * immune_magic_attack_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                context.attackResult = CombatContext.ATTACKRESULT_IMMUNE;
                effectTimes++;
            }
        }
        if (!isActive && context.hited() && context.skill instanceof SlowSkill) {
            int rate = (int)(multiple * 100 * immune_slow_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                context.attackResult = CombatContext.ATTACKRESULT_IMMUNE;
                effectTimes++;
            }
        }
        if (!isActive && context.hited() && context.skill instanceof FearSkill) {
            int rate = (int)(multiple * 100 * immune_fear_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                context.attackResult = CombatContext.ATTACKRESULT_IMMUNE;
                effectTimes++;
            }
        }
        if (!isActive && context.hited() && context.skill instanceof DumbSkill) {
            int rate = (int)(multiple * 100 * immune_dumb_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                context.attackResult = CombatContext.ATTACKRESULT_IMMUNE;
                effectTimes++;
            }
        }
        if (!isActive && context.hited() && context.skill instanceof ParalyzeSkill) {
            int rate = (int)(multiple * 100 * immune_paralyze_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                context.attackResult = CombatContext.ATTACKRESULT_IMMUNE;
                effectTimes++;
            }
        }
        if (!isActive && context.hited() && context.skill instanceof StaySkill) {
            int rate = (int)(multiple * 100 * immune_stay_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                context.attackResult = CombatContext.ATTACKRESULT_IMMUNE;
                effectTimes++;
            }
        }
        if (!isActive && context.hited() && context.skill instanceof BreakAttackSkill) {
            int rate = (int)(multiple * 100 * immune_break_rate);
            if (CommonUtil.hit(RND, rate, 10000)) {
                context.attackResult = CombatContext.ATTACKRESULT_IMMUNE;
                effectTimes++;
            }
        }
    }

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

}
