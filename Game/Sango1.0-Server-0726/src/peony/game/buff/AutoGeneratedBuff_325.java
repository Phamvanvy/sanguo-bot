package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_325 implements Buff, SkillEnhancer, Updatable {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, 900000, 900000, 900000, 900000, 900000
    };
    public static String[] CHANGE_MP_SKILLS = {
        "", "63", "63", "63", "63", "63"
    };
    public static float[] CHANGE_MP_PERCENT = {
        0.0f, -6.0f, -12.0f, -18.0f, -24.0f, -30.0f
    };
    int instanceID;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    String change_mp_skills;
    float change_mp_percent;

    public AutoGeneratedBuff_325(int lvl, Unit src, Unit tgt, int dmg) {
        if (src != null) {
            source = src.ref();
        }
        if (tgt != null) {
            owner = tgt.ref();
        }
        instanceID = BuffUtil.getNextID();
        level = lvl;
        endTime = Time.currTime + BuffUtil.enhanceParam(src, tgt, 325, "buffTime", DURATION[level]);
        multiple = 1;
        change_mp_skills = CHANGE_MP_SKILLS[level];
        change_mp_percent = CHANGE_MP_PERCENT[level];
        change_mp_percent = BuffUtil.enhanceParam(src, tgt, 325, "change_mp_percent", change_mp_percent);
    }

    public int getId() {
        return 325;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "强化镇心理气";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("施放镇心理气时，精力/气力消耗减少{0}", CommonUtil.formatPercent(change_mp_percent * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return endTime;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return true;
    }

    public int getIconID() {
        return 40 | 0x1000000;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_325) {
            AutoGeneratedBuff_325 other = (AutoGeneratedBuff_325)buff;
            if (level <= other.level) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure = other.remainCure;
                remainDamage = other.remainDamage;
                remainMPCure = other.remainMPCure;
                remainMPDamage = other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                effectTimes = 0;
                owner = other.owner;
                source = other.source;
                change_mp_skills = other.change_mp_skills;
                change_mp_percent = other.change_mp_percent;
            }
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
    }

    public boolean update(int diff) {
        if (endTime <= Time.currTime) {
            return true;
        }
        return false;
    }

    public void load(byte[] bytes) {
        ByteBuffer buf = ByteBuffer.wrap(bytes);
        owner = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        source = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        level = buf.getInt();
        endTime = Time.elapseTime(buf.getLong());
        multiple = buf.getInt();
        change_mp_skills = CHANGE_MP_SKILLS[level];
        change_mp_percent = CHANGE_MP_PERCENT[level];
        remainSeconds = buf.getInt();
        tickInterval = buf.getInt();
        remainCure = buf.getInt();
        remainDamage = buf.getInt();
        remainMPCure = buf.getInt();
        remainMPDamage = buf.getInt();
        remainAbsorb = buf.getInt();
        effectTimes = buf.getInt();
    }

    public byte[] save() {
        ByteBuffer buf = ByteBuffer.allocate(66, false);
        buf.put(owner.type);
        buf.putInt(owner.id);
        buf.putInt(owner.instanceId);
        buf.put(source.type);
        buf.putInt(source.id);
        buf.putInt(source.instanceId);
        buf.putInt(level);
        buf.putLong(Time.currentTimeMillis(endTime));
        buf.putInt(multiple);
        buf.putInt(remainSeconds);
        buf.putInt(tickInterval);
        buf.putInt(remainCure);
        buf.putInt(remainDamage);
        buf.putInt(remainMPCure);
        buf.putInt(remainMPDamage);
        buf.putInt(remainAbsorb);
        buf.putInt(effectTimes);
        return buf.array();
    }

    public boolean update2(int time) {
        if (endTime <= Time.currTime) {
            return false;
        }
        return true;
    }

    private IntHashSet changeSkills;
    public IntHashSet getAffectSkillIDs() {
        if (changeSkills == null) {
            int[] idarr;
            changeSkills = new IntHashSet();
            idarr = Utils.stringToIntArray(change_mp_skills, ',');
            for (int sid : idarr) {
                changeSkills.add(sid);
            }
        }
        return changeSkills;
    }

    public float updateCDTime(Skill skill, float cd) {
            return cd;
    }

    public float updateDistance(Skill skill, float distance) {
            return distance;
    }

    public float updateActTime(Skill skill, float actTime) {
            return actTime;
    }

    public float updateRange(Skill skill, float range) {
            return range;
    }

    public float updateMP(Skill skill, float mp) {
            return mp * (1.0f + change_mp_percent / 100.0f);
    }

}
