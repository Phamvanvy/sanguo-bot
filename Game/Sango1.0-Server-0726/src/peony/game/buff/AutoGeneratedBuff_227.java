package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_227 implements Buff, SkillEnhancer {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, -1
    };
    public static String[] CHANGE_MP_SKILLS = {
        "", "66"
    };
    public static float[] CHANGE_MP_PERCENT = {
        0.0f, 20.0f
    };
    int instanceID;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    String change_mp_skills;
    float change_mp_percent;

    public AutoGeneratedBuff_227(int lvl) {
        int dmg = 0;
        instanceID = BuffUtil.getNextID();
        level = lvl;
        multiple = 1;
        change_mp_skills = CHANGE_MP_SKILLS[level];
        change_mp_percent = CHANGE_MP_PERCENT[level];
    }

    public int getId() {
        return 227;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "降低碧海潮生耗蓝";
    }

    public String getDesc() {
        return "降低碧海潮生技能消耗20%";
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return -1;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return -1;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_227) {
            AutoGeneratedBuff_227 other = (AutoGeneratedBuff_227)buff;
            if (level <= other.level) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure = other.remainCure;
                remainDamage = other.remainDamage;
                remainMPCure = other.remainMPCure;
                remainMPDamage = other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                effectTimes = 0;
                owner = other.owner;
                source = other.source;
                change_mp_skills = other.change_mp_skills;
                change_mp_percent = other.change_mp_percent;
            }
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
        change_mp_percent = CHANGE_MP_PERCENT[level];
        change_mp_percent = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 227, "change_mp_percent", change_mp_percent);
    }

    private IntHashSet changeSkills;
    public IntHashSet getAffectSkillIDs() {
        if (changeSkills == null) {
            int[] idarr;
            changeSkills = new IntHashSet();
            idarr = Utils.stringToIntArray(change_mp_skills, ',');
            for (int sid : idarr) {
                changeSkills.add(sid);
            }
        }
        return changeSkills;
    }

    public float updateCDTime(Skill skill, float cd) {
            return cd;
    }

    public float updateDistance(Skill skill, float distance) {
            return distance;
    }

    public float updateActTime(Skill skill, float actTime) {
            return actTime;
    }

    public float updateRange(Skill skill, float range) {
            return range;
    }

    public float updateMP(Skill skill, float mp) {
            return mp * (1.0f + change_mp_percent / 100.0f);
    }

}
