package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_435 implements Buff, PropertyEnhancer {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, -1
    };
    public static float[] CHANGE_PHYSICAL_CRIT = {
        0.0f, 5.0f
    };
    public static float[] CHANGE_MAGIC_CRIT = {
        0.0f, 5.0f
    };
    public static float[] CHANGE_CRITED = {
        0.0f, 5.0f
    };
    int instanceID;
    int realWeight;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    float change_physical_crit;
    float change_magic_crit;
    float change_crited;


    public AutoGeneratedBuff_435(int lvl) {
        int dmg = 0;
        instanceID = BuffUtil.getNextID();
        level = lvl;
        multiple = 1;
        change_physical_crit = CHANGE_PHYSICAL_CRIT[level];
        change_magic_crit = CHANGE_MAGIC_CRIT[level];
        change_crited = CHANGE_CRITED[level];
    }

    public int getId() {
        return 435;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "八星星辉(坐骑)";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("物理和法术暴击增加{0}；免暴增加{1}", CommonUtil.formatPercent(change_physical_crit * multiple), CommonUtil.formatPercent(change_crited * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return -1;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return -1;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public int getMergeStrategy() {
        return 2;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public float getchange_dembuff_rate() {
         return 0;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_435) {
            AutoGeneratedBuff_435 other = (AutoGeneratedBuff_435)buff;
            if (level <= other.level) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure = other.remainCure;
                remainDamage = other.remainDamage;
                remainMPCure = other.remainMPCure;
                remainMPDamage = other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                effectTimes = 0;
                owner = other.owner;
                source = other.source;
                change_physical_crit = other.change_physical_crit;
                change_magic_crit = other.change_magic_crit;
                change_crited = other.change_crited;
            }
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
        change_physical_crit = CHANGE_PHYSICAL_CRIT[level];
        change_physical_crit = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 435, "change_physical_crit", change_physical_crit);
        change_magic_crit = CHANGE_MAGIC_CRIT[level];
        change_magic_crit = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 435, "change_magic_crit", change_magic_crit);
        change_crited = CHANGE_CRITED[level];
        change_crited = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 435, "change_crited", change_crited);
    }

    public void enhance(PropertyCalculator pc) {
        if (!checkWeapon(pc.unit)) {
            return;
        }
        pc.critical += multiple * change_physical_crit / 100.0f;
        pc.spellcritical += multiple * change_magic_crit / 100.0f;
        pc.anticrit += multiple * change_crited / 100.0f;
    }

}
