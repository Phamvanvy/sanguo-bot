package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_174 implements Buff, PropertyEnhancer {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, -1
    };
    public static float[] CHANGE_WEAPON_ATK_VALUE = {
        0.0f, 4.0f
    };
    public static float[] CHANGE_WEAPON_MATK_VALUE = {
        0.0f, 4.0f
    };
    int instanceID;
    int realWeight;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    float change_weapon_atk_value;
    float change_weapon_matk_value;


    public AutoGeneratedBuff_174(int lvl) {
        int dmg = 0;
        instanceID = BuffUtil.getNextID();
        level = lvl;
        multiple = 1;
        change_weapon_atk_value = CHANGE_WEAPON_ATK_VALUE[level];
        change_weapon_matk_value = CHANGE_WEAPON_MATK_VALUE[level];
    }

    public int getId() {
        return 174;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "结义攻击4%";
    }

    public String getDesc() {
        return "武器提供的物攻法攻提升4%";
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return -1;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return -1;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public int getMergeStrategy() {
        return 2;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public float getchange_dembuff_rate() {
         return 0;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_174) {
            AutoGeneratedBuff_174 other = (AutoGeneratedBuff_174)buff;
            if (level <= other.level) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure = other.remainCure;
                remainDamage = other.remainDamage;
                remainMPCure = other.remainMPCure;
                remainMPDamage = other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                effectTimes = 0;
                owner = other.owner;
                source = other.source;
                change_weapon_atk_value = other.change_weapon_atk_value;
                change_weapon_matk_value = other.change_weapon_matk_value;
            }
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
        change_weapon_atk_value = CHANGE_WEAPON_ATK_VALUE[level];
        change_weapon_atk_value = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 174, "change_weapon_atk_value", change_weapon_atk_value);
        change_weapon_matk_value = CHANGE_WEAPON_MATK_VALUE[level];
        change_weapon_matk_value = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 174, "change_weapon_matk_value", change_weapon_matk_value);
    }

    public void enhance(PropertyCalculator pc) {
        if (!checkWeapon(pc.unit)) {
            return;
        }
        if (pc.unit.equipments.getWeapon() != null) {
            GameItem t = pc.unit.equipments.getWeapon();
            pc.attackpowerup += t.getMaxAttack() * multiple * change_weapon_atk_value / 100.0f;
            pc.attackpowerdown += t.getMinAttack() * multiple * change_weapon_atk_value / 100.0f;
        }
        if (pc.unit.equipments.getWeapon() != null) {
            GameItem t = pc.unit.equipments.getWeapon();
            pc.spellpower += t.getMagicPower() * multiple * change_weapon_matk_value / 100.0f;
        }
    }

}
