package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_284 implements Buff, PropertyEnhancer, Updatable, CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, 15000, 15000, 15000, 15000, 15000
    };
    public static float[] ADD_SPEED_PERCENT = {
        0.0f, 30.0f, 60.0f, 120.0f, 240.0f, 480.0f
    };
    public static float[] SLOW_RATE = {
        0.0f, 100.0f, 100.0f, 100.0f, 100.0f, 100.0f
    };
    public static String[] SLOW_RATE_VAR = {
        "", "", "", "", "", ""
    };
    public static int[] SLOW_LEVEL = {
        0, 50, 50, 50, 50, 50
    };
    public static String[] SLOW_LEVEL_VAR = {
        "", "", "", "", "", ""
    };
    public static int[] SLOW_TIME = {
        0, 5000, 5000, 5000, 5000, 5000
    };
    public static String[] SLOW_TIME_VAR = {
        "", "", "", "", "", ""
    };
    int instanceID;
    int realWeight;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    float add_speed_percent;
    float slow_rate;
    String slow_rate_var;
    int slow_level;
    String slow_level_var;
    int slow_time;
    String slow_time_var;

    public AutoGeneratedBuff_284(int lvl, Unit src, Unit tgt, int dmg) {
        if (src != null) {
            source = src.ref();
        }
        if (tgt != null) {
            owner = tgt.ref();
        }
        instanceID = BuffUtil.getNextID();
        level = lvl;
        endTime = Time.currTime + BuffUtil.enhanceParam(src, tgt, 284, "buffTime", DURATION[level]);
        multiple = 1;
        add_speed_percent = ADD_SPEED_PERCENT[level];
        add_speed_percent = BuffUtil.enhanceParam(src, tgt, 284, "add_speed_percent", add_speed_percent);
        slow_rate = SLOW_RATE[level];
        slow_rate = BuffUtil.enhanceParam(src, tgt, 284, "slow_rate", slow_rate);
        slow_rate_var = SLOW_RATE_VAR[level];
        slow_level = SLOW_LEVEL[level];
        slow_level = BuffUtil.enhanceParam(src, tgt, 284, "slow_level", slow_level);
        slow_level_var = SLOW_LEVEL_VAR[level];
        slow_time = SLOW_TIME[level];
        slow_time = BuffUtil.enhanceParam(src, tgt, 284, "slow_time", slow_time);
        slow_time_var = SLOW_TIME_VAR[level];
    }

    public int getId() {
        return 284;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "加速冰攻";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("移动速度增加了{0}，命中目标后使目标5秒内减速{1}", CommonUtil.formatPercent(add_speed_percent * multiple), CommonUtil.formatPercent(slow_level * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return endTime;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return 44 | 0x1000000;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public int getMergeStrategy() {
        return 3;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public float getchange_dembuff_rate() {
         return 0;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_284) {
            AutoGeneratedBuff_284 other = (AutoGeneratedBuff_284)buff;
            if (source.equals(other.source)) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure += other.remainCure;
                remainDamage += other.remainDamage;
                remainMPCure += other.remainMPCure;
                remainMPDamage += other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                add_speed_percent = other.add_speed_percent;
                slow_rate = other.slow_rate;
                slow_rate_var = other.slow_rate_var;
                slow_level = other.slow_level;
                slow_level_var = other.slow_level_var;
                slow_time = other.slow_time;
                slow_time_var = other.slow_time_var;
                return true;
            }
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
    }

    public boolean update(int diff) {
        if (endTime <= Time.currTime) {
            return true;
        }
        return false;
    }

    public void load(byte[] bytes) {
        ByteBuffer buf = ByteBuffer.wrap(bytes);
        owner = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        source = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        level = buf.getInt();
        endTime = Time.elapseTime(buf.getLong());
        multiple = buf.getInt();
        add_speed_percent = ADD_SPEED_PERCENT[level];
        slow_rate = SLOW_RATE[level];
        slow_rate_var = SLOW_RATE_VAR[level];
        slow_level = SLOW_LEVEL[level];
        slow_level_var = SLOW_LEVEL_VAR[level];
        slow_time = SLOW_TIME[level];
        slow_time_var = SLOW_TIME_VAR[level];
        remainSeconds = buf.getInt();
        tickInterval = buf.getInt();
        remainCure = buf.getInt();
        remainDamage = buf.getInt();
        remainMPCure = buf.getInt();
        remainMPDamage = buf.getInt();
        remainAbsorb = buf.getInt();
        effectTimes = buf.getInt();
    }

    public byte[] save() {
        ByteBuffer buf = ByteBuffer.allocate(66, false);
        buf.put(owner.type);
        buf.putInt(owner.id);
        buf.putInt(owner.instanceId);
        buf.put(source.type);
        buf.putInt(source.id);
        buf.putInt(source.instanceId);
        buf.putInt(level);
        buf.putLong(Time.currentTimeMillis(endTime));
        buf.putInt(multiple);
        buf.putInt(remainSeconds);
        buf.putInt(tickInterval);
        buf.putInt(remainCure);
        buf.putInt(remainDamage);
        buf.putInt(remainMPCure);
        buf.putInt(remainMPDamage);
        buf.putInt(remainAbsorb);
        buf.putInt(effectTimes);
        return buf.array();
    }

    public boolean update2(int time) {
        endTime += time;
        if (endTime <= Time.currTime) {
            return false;
        }
        return true;
    }

    public void enhance(PropertyCalculator pc) {
        if (!checkWeapon(pc.unit)) {
            return;
        }
        if (add_speed_percent > 0.0f) {
            pc.fast2(multiple * add_speed_percent / 100.0f);
        }
    }

    public void preHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (isActive && context.hited() && context.isDamage()) {
            boolean hit = false;
            if (slow_rate_var.length() == 0) {
                hit = CommonUtil.hit(RND, (int)(multiple * 100 * slow_rate), 10000);
            } else {
                Float vo = context.skillParams.get(slow_rate_var);
                if (vo != null) {
                    hit = CommonUtil.hit(RND, (int)(multiple * 100 * (vo.floatValue() + slow_rate)), 10000);
                }
            }
            if (hit) {
                int sl = slow_level;
                Float vo = context.skillParams.get(slow_level_var);
                if (vo != null) {
                    sl += vo.intValue();
                }
                int tm = slow_time;
                vo = context.skillParams.get(slow_time_var);
                if (vo != null) {
                    tm *= 1.0f + vo.floatValue() / 100.0f;
                }
                context.activeSkills.add(new SlowSkill(sl, tm));
                effectTimes++;
            }
        }
    }

}
