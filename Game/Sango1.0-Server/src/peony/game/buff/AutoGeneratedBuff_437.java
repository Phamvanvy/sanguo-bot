package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_437 implements Buff, Updatable {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, -1
    };
    public static int[] HOT_SECONDS = {
        0, 1
    };
    public static int[] HOT_TICK_TIME = {
        0, 1
    };
    public static int[] HOT_VALUE = {
        0, 6000
    };
    public static float[] HOT_PERCENT_OF_DAMAGE = {
        0.0f, 0.0f
    };
    public static int[] MPHOT_SECONDS = {
        0, 1
    };
    public static int[] MPHOT_TICK_TIME = {
        0, 1
    };
    public static int[] MPHOT_VALUE = {
        0, 3000
    };
    public static float[] MPHOT_PERCENT_OF_DAMAGE = {
        0.0f, 0.0f
    };
    int instanceID;
    int realWeight;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    int hot_seconds;
    int hot_tick_time;
    int hot_value;
    float hot_percent_of_damage;
    int mphot_seconds;
    int mphot_tick_time;
    int mphot_value;
    float mphot_percent_of_damage;

    public AutoGeneratedBuff_437(int lvl, Unit src, Unit tgt, int dmg) {
        if (src != null) {
            source = src.ref();
        }
        if (tgt != null) {
            owner = tgt.ref();
        }
        instanceID = BuffUtil.getNextID();
        level = lvl;
        endTime = Time.currTime + BuffUtil.enhanceParam(src, tgt, 437, "buffTime", DURATION[level]);
        multiple = 1;
        hot_seconds = HOT_SECONDS[level];
        hot_seconds = BuffUtil.enhanceParam(src, tgt, 437, "hot_seconds", hot_seconds);
        hot_tick_time = HOT_TICK_TIME[level];
        hot_tick_time = BuffUtil.enhanceParam(src, tgt, 437, "hot_tick_time", hot_tick_time);
        hot_value = HOT_VALUE[level];
        hot_value = BuffUtil.enhanceParam(src, tgt, 437, "hot_value", hot_value);
        hot_percent_of_damage = HOT_PERCENT_OF_DAMAGE[level];
        hot_percent_of_damage = BuffUtil.enhanceParam(src, tgt, 437, "hot_percent_of_damage", hot_percent_of_damage);
        mphot_seconds = MPHOT_SECONDS[level];
        mphot_seconds = BuffUtil.enhanceParam(src, tgt, 437, "mphot_seconds", mphot_seconds);
        mphot_tick_time = MPHOT_TICK_TIME[level];
        mphot_tick_time = BuffUtil.enhanceParam(src, tgt, 437, "mphot_tick_time", mphot_tick_time);
        mphot_value = MPHOT_VALUE[level];
        mphot_value = BuffUtil.enhanceParam(src, tgt, 437, "mphot_value", mphot_value);
        mphot_percent_of_damage = MPHOT_PERCENT_OF_DAMAGE[level];
        mphot_percent_of_damage = BuffUtil.enhanceParam(src, tgt, 437, "mphot_percent_of_damage", mphot_percent_of_damage);
        remainSeconds = hot_seconds;
        tickInterval = hot_tick_time;
        remainCure = hot_value;
        remainCure += hot_percent_of_damage * dmg / 100.0f;
        remainMPCure = mphot_value;
        remainMPCure += mphot_percent_of_damage * dmg / 100.0f;
    }

    public int getId() {
        return 437;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "回血回蓝";
    }

    public String getDesc() {
        return "回复生命和气力/精力";
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return Time.currTime + remainSeconds * 1000;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return 110 | 0x1000000;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public int getMergeStrategy() {
        return 4;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public float getchange_dembuff_rate() {
         return 0;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_437) {
            AutoGeneratedBuff_437 other = (AutoGeneratedBuff_437)buff;
            level = other.level;
            endTime = other.endTime;
            multiple = 1;
            remainSeconds = other.remainSeconds;
            tickInterval = other.tickInterval;
            remainCure = other.remainCure;
            remainDamage = other.remainDamage;
            remainMPCure = other.remainMPCure;
            remainMPDamage = other.remainMPDamage;
            remainAbsorb = other.remainAbsorb;
            effectTimes = 0;
            owner = other.owner;
            source = other.source;
            hot_seconds = other.hot_seconds;
            hot_tick_time = other.hot_tick_time;
            hot_value = other.hot_value;
            hot_percent_of_damage = other.hot_percent_of_damage;
            mphot_seconds = other.mphot_seconds;
            mphot_tick_time = other.mphot_tick_time;
            mphot_value = other.mphot_value;
            mphot_percent_of_damage = other.mphot_percent_of_damage;
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
    }

    public boolean update(int diff) {
        if (((remainSeconds - 1) % tickInterval) == 0) {
            int remainTicks = (remainSeconds - 1) / tickInterval + 1;
            int cure = remainCure / remainTicks;
            int dmg = remainDamage / remainTicks;
            int mpcure = remainMPCure / remainTicks;
            int mpdmg = remainMPDamage / remainTicks;
            remainCure -= cure;
            remainDamage -= dmg;
            remainMPCure -= mpcure;
            remainMPDamage -= mpdmg;
            if (cure > 0) {
                Unit healerUnit = (Unit)ObjectAccessor.getGameObject(source);
                Unit targetUnit = (Unit)ObjectAccessor.getGameObject(owner);
                if (targetUnit != null && targetUnit.isAlive()) {
                    cure = targetUnit.setHp(targetUnit.hp + cure, true);
                    if (healerUnit != null && healerUnit.isAlive()) {
                        Attack.addHealThreat(healerUnit, targetUnit, CombatContext.calcHotThreat(cure), false);
                    }
                }
            }
            if (dmg > 0) {
                Unit sourceUnit = (Unit)ObjectAccessor.getGameObject(source);
                Unit targetUnit = (Unit)ObjectAccessor.getGameObject(owner);
                if (targetUnit != null && targetUnit.isAlive()) {
                    targetUnit.setHp(targetUnit.hp - dmg, true);
                    if (sourceUnit != null && sourceUnit.isAlive()) {
                        Attack.addDamageThreat(sourceUnit, targetUnit, dmg, false);
                    }
                    if (targetUnit.hp <= 0) {
                        targetUnit.die(sourceUnit);
                    }
                }
            }
            if (mpcure > 0) {
                Unit healerUnit = (Unit)ObjectAccessor.getGameObject(source);
                Unit targetUnit = (Unit)ObjectAccessor.getGameObject(owner);
                if (targetUnit != null && targetUnit.isAlive()) {
                    targetUnit.setMp(targetUnit.mp + mpcure, true);
                    if (healerUnit != null && healerUnit.isAlive()) {
                        Attack.addHealThreat(healerUnit, targetUnit, mpcure, false);
                    }
                }
            }
            if (mpdmg > 0) {
                Unit sourceUnit = (Unit)ObjectAccessor.getGameObject(source);
                Unit targetUnit = (Unit)ObjectAccessor.getGameObject(owner);
                if (targetUnit != null && targetUnit.isAlive()) {
                    targetUnit.setMp(targetUnit.mp - mpdmg, true);
                    if (sourceUnit != null && sourceUnit.isAlive()) {
                        Attack.addDamageThreat(sourceUnit, targetUnit, mpdmg, false);
                    }
                }
            }
        }
        remainSeconds--;
        if (remainSeconds <= 0) {
            return true;
        }
        return false;
    }

    public void load(byte[] bytes) {
        ByteBuffer buf = ByteBuffer.wrap(bytes);
        owner = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        source = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        level = buf.getInt();
        endTime = Time.elapseTime(buf.getLong());
        multiple = buf.getInt();
        hot_seconds = HOT_SECONDS[level];
        hot_tick_time = HOT_TICK_TIME[level];
        hot_value = HOT_VALUE[level];
        hot_percent_of_damage = HOT_PERCENT_OF_DAMAGE[level];
        mphot_seconds = MPHOT_SECONDS[level];
        mphot_tick_time = MPHOT_TICK_TIME[level];
        mphot_value = MPHOT_VALUE[level];
        mphot_percent_of_damage = MPHOT_PERCENT_OF_DAMAGE[level];
        remainSeconds = buf.getInt();
        tickInterval = buf.getInt();
        remainCure = buf.getInt();
        remainDamage = buf.getInt();
        remainMPCure = buf.getInt();
        remainMPDamage = buf.getInt();
        remainAbsorb = buf.getInt();
        effectTimes = buf.getInt();
    }

    public byte[] save() {
        ByteBuffer buf = ByteBuffer.allocate(66, false);
        buf.put(owner.type);
        buf.putInt(owner.id);
        buf.putInt(owner.instanceId);
        buf.put(source.type);
        buf.putInt(source.id);
        buf.putInt(source.instanceId);
        buf.putInt(level);
        buf.putLong(Time.currentTimeMillis(endTime));
        buf.putInt(multiple);
        buf.putInt(remainSeconds);
        buf.putInt(tickInterval);
        buf.putInt(remainCure);
        buf.putInt(remainDamage);
        buf.putInt(remainMPCure);
        buf.putInt(remainMPDamage);
        buf.putInt(remainAbsorb);
        buf.putInt(effectTimes);
        return buf.array();
    }

    public boolean update2(int time) {
        if (remainSeconds <= 0) {
            return false;
        }
        return true;
    }

}
