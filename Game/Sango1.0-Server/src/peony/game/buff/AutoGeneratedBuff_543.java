package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_543 implements Buff, PropertyEnhancer, Updatable, CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, 10000
    };
    public static int[] CHANGE_THREAT_VALUE = {
        0, 0
    };
    public static float[] CHANGE_THREAT_PERCENT = {
        0.0f, 100.0f
    };
    public static int[] REDUCE_PHYSICAL_DAMAGE_VALUE = {
        0, 0
    };
    public static float[] REDUCE_PHYSICAL_DAMAGE_PERCENT = {
        0.0f, 50.0f
    };
    public static int[] REDUCE_MAGIC_DAMAGE_VALUE = {
        0, 0
    };
    public static float[] REDUCE_MAGIC_DAMAGE_PERCENT = {
        0.0f, 50.0f
    };
    public static float[] CHANGE_SPEED_PERCENT = {
        0.0f, -100.0f
    };
    public static int[] MPDOT_SECONDS = {
        0, 10
    };
    public static int[] MPDOT_TICK_TIME = {
        0, 2
    };
    public static int[] MPDOT_VALUE = {
        0, 2000
    };
    public static float[] MPDOT_PERCENT_OF_DAMAGE = {
        0.0f, 0.0f
    };
    public static float[] STAY_RATE = {
        0.0f, 100.0f
    };
    int instanceID;
    int realWeight;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    int change_threat_value;
    float change_threat_percent;
    int reduce_physical_damage_value;
    float reduce_physical_damage_percent;
    int reduce_magic_damage_value;
    float reduce_magic_damage_percent;
    float change_speed_percent;
    int mpdot_seconds;
    int mpdot_tick_time;
    int mpdot_value;
    float mpdot_percent_of_damage;
    float stay_rate;

    public AutoGeneratedBuff_543(int lvl, Unit src, Unit tgt, int dmg) {
        if (src != null) {
            source = src.ref();
        }
        if (tgt != null) {
            owner = tgt.ref();
        }
        instanceID = BuffUtil.getNextID();
        level = lvl;
        endTime = Time.currTime + BuffUtil.enhanceParam(src, tgt, 543, "buffTime", DURATION[level]);
        if(owner!=null){
           Player player = ObjectAccessor.getPlayer(owner.id);
           if(player!=null)
               player.buffs.addBuff(new StayDebuff(player, endTime-Time.currTime<=0?0:endTime-Time.currTime));
        }
        multiple = 1;
        change_threat_value = CHANGE_THREAT_VALUE[level];
        change_threat_value = BuffUtil.enhanceParam(src, tgt, 543, "change_threat_value", change_threat_value);
        change_threat_percent = CHANGE_THREAT_PERCENT[level];
        change_threat_percent = BuffUtil.enhanceParam(src, tgt, 543, "change_threat_percent", change_threat_percent);
        reduce_physical_damage_value = REDUCE_PHYSICAL_DAMAGE_VALUE[level];
        reduce_physical_damage_value = BuffUtil.enhanceParam(src, tgt, 543, "reduce_physical_damage_value", reduce_physical_damage_value);
        reduce_physical_damage_percent = REDUCE_PHYSICAL_DAMAGE_PERCENT[level];
        reduce_physical_damage_percent = BuffUtil.enhanceParam(src, tgt, 543, "reduce_physical_damage_percent", reduce_physical_damage_percent);
        reduce_magic_damage_value = REDUCE_MAGIC_DAMAGE_VALUE[level];
        reduce_magic_damage_value = BuffUtil.enhanceParam(src, tgt, 543, "reduce_magic_damage_value", reduce_magic_damage_value);
        reduce_magic_damage_percent = REDUCE_MAGIC_DAMAGE_PERCENT[level];
        reduce_magic_damage_percent = BuffUtil.enhanceParam(src, tgt, 543, "reduce_magic_damage_percent", reduce_magic_damage_percent);
        change_speed_percent = CHANGE_SPEED_PERCENT[level];
        change_speed_percent = BuffUtil.enhanceParam(src, tgt, 543, "change_speed_percent", change_speed_percent);
        mpdot_seconds = MPDOT_SECONDS[level];
        mpdot_seconds = BuffUtil.enhanceParam(src, tgt, 543, "mpdot_seconds", mpdot_seconds);
        mpdot_tick_time = MPDOT_TICK_TIME[level];
        mpdot_tick_time = BuffUtil.enhanceParam(src, tgt, 543, "mpdot_tick_time", mpdot_tick_time);
        mpdot_value = MPDOT_VALUE[level];
        mpdot_value = BuffUtil.enhanceParam(src, tgt, 543, "mpdot_value", mpdot_value);
        mpdot_percent_of_damage = MPDOT_PERCENT_OF_DAMAGE[level];
        mpdot_percent_of_damage = BuffUtil.enhanceParam(src, tgt, 543, "mpdot_percent_of_damage", mpdot_percent_of_damage);
        stay_rate = STAY_RATE[level];
        stay_rate = BuffUtil.enhanceParam(src, tgt, 543, "stay_rate", stay_rate);
        remainSeconds = mpdot_seconds;
        tickInterval = mpdot_tick_time;
        remainMPDamage = mpdot_value;
        remainMPDamage += mpdot_percent_of_damage * dmg / 100.0f;
    }

    public int getId() {
        return 543;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "大火";
    }

    public String getDesc() {
        return "10秒内烧掉2000蓝，不可移动；同时提高50%伤害减免，提高100%仇恨";
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return Time.currTime + remainSeconds * 1000;
    }

    public boolean isGood() {
        return false;
    }

    public boolean dispelable() {
        return true;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return 27 | 0x1000000;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public int getMergeStrategy() {
        return 4;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public float getchange_dembuff_rate() {
         return 0;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_543) {
            AutoGeneratedBuff_543 other = (AutoGeneratedBuff_543)buff;
            level = other.level;
            endTime = other.endTime;
            multiple = 1;
            remainSeconds = other.remainSeconds;
            tickInterval = other.tickInterval;
            remainCure = other.remainCure;
            remainDamage = other.remainDamage;
            remainMPCure = other.remainMPCure;
            remainMPDamage = other.remainMPDamage;
            remainAbsorb = other.remainAbsorb;
            effectTimes = 0;
            owner = other.owner;
            source = other.source;
            change_threat_value = other.change_threat_value;
            change_threat_percent = other.change_threat_percent;
            reduce_physical_damage_value = other.reduce_physical_damage_value;
            reduce_physical_damage_percent = other.reduce_physical_damage_percent;
            reduce_magic_damage_value = other.reduce_magic_damage_value;
            reduce_magic_damage_percent = other.reduce_magic_damage_percent;
            change_speed_percent = other.change_speed_percent;
            mpdot_seconds = other.mpdot_seconds;
            mpdot_tick_time = other.mpdot_tick_time;
            mpdot_value = other.mpdot_value;
            mpdot_percent_of_damage = other.mpdot_percent_of_damage;
            stay_rate = other.stay_rate;
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
    }

    public boolean update(int diff) {
        if (((remainSeconds - 1) % tickInterval) == 0) {
            int remainTicks = (remainSeconds - 1) / tickInterval + 1;
            int cure = remainCure / remainTicks;
            int dmg = remainDamage / remainTicks;
            int mpcure = remainMPCure / remainTicks;
            int mpdmg = remainMPDamage / remainTicks;
            remainCure -= cure;
            remainDamage -= dmg;
            remainMPCure -= mpcure;
            remainMPDamage -= mpdmg;
            if (cure > 0) {
                Unit healerUnit = (Unit)ObjectAccessor.getGameObject(source);
                Unit targetUnit = (Unit)ObjectAccessor.getGameObject(owner);
                if (targetUnit != null && targetUnit.isAlive()) {
                    cure = targetUnit.setHp(targetUnit.hp + cure, true);
                    if (healerUnit != null && healerUnit.isAlive()) {
                        Attack.addHealThreat(healerUnit, targetUnit, CombatContext.calcHotThreat(cure), false);
                    }
                }
            }
            if (dmg > 0) {
                Unit sourceUnit = (Unit)ObjectAccessor.getGameObject(source);
                Unit targetUnit = (Unit)ObjectAccessor.getGameObject(owner);
                if (targetUnit != null && targetUnit.isAlive()) {
                    targetUnit.setHp(targetUnit.hp - dmg, true);
                    if (sourceUnit != null && sourceUnit.isAlive()) {
                        Attack.addDamageThreat(sourceUnit, targetUnit, dmg, false);
                    }
                    if (targetUnit.hp <= 0) {
                        targetUnit.die(sourceUnit);
                    }
                }
            }
            if (mpcure > 0) {
                Unit healerUnit = (Unit)ObjectAccessor.getGameObject(source);
                Unit targetUnit = (Unit)ObjectAccessor.getGameObject(owner);
                if (targetUnit != null && targetUnit.isAlive()) {
                    targetUnit.setMp(targetUnit.mp + mpcure, true);
                    if (healerUnit != null && healerUnit.isAlive()) {
                        Attack.addHealThreat(healerUnit, targetUnit, mpcure, false);
                    }
                }
            }
            if (mpdmg > 0) {
                Unit sourceUnit = (Unit)ObjectAccessor.getGameObject(source);
                Unit targetUnit = (Unit)ObjectAccessor.getGameObject(owner);
                if (targetUnit != null && targetUnit.isAlive()) {
                    targetUnit.setMp(targetUnit.mp - mpdmg, true);
                    if (sourceUnit != null && sourceUnit.isAlive()) {
                        Attack.addDamageThreat(sourceUnit, targetUnit, mpdmg, false);
                    }
                }
            }
        }
        remainSeconds--;
        if (remainSeconds <= 0) {
            return true;
        }
        if (endTime <= Time.currTime) {
            return true;
        }
        return false;
    }

    public void load(byte[] bytes) {
        ByteBuffer buf = ByteBuffer.wrap(bytes);
        owner = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        source = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        level = buf.getInt();
        endTime = Time.elapseTime(buf.getLong());
        multiple = buf.getInt();
        change_threat_value = CHANGE_THREAT_VALUE[level];
        change_threat_percent = CHANGE_THREAT_PERCENT[level];
        reduce_physical_damage_value = REDUCE_PHYSICAL_DAMAGE_VALUE[level];
        reduce_physical_damage_percent = REDUCE_PHYSICAL_DAMAGE_PERCENT[level];
        reduce_magic_damage_value = REDUCE_MAGIC_DAMAGE_VALUE[level];
        reduce_magic_damage_percent = REDUCE_MAGIC_DAMAGE_PERCENT[level];
        change_speed_percent = CHANGE_SPEED_PERCENT[level];
        mpdot_seconds = MPDOT_SECONDS[level];
        mpdot_tick_time = MPDOT_TICK_TIME[level];
        mpdot_value = MPDOT_VALUE[level];
        mpdot_percent_of_damage = MPDOT_PERCENT_OF_DAMAGE[level];
        stay_rate = STAY_RATE[level];
        remainSeconds = buf.getInt();
        tickInterval = buf.getInt();
        remainCure = buf.getInt();
        remainDamage = buf.getInt();
        remainMPCure = buf.getInt();
        remainMPDamage = buf.getInt();
        remainAbsorb = buf.getInt();
        effectTimes = buf.getInt();
    }

    public byte[] save() {
        ByteBuffer buf = ByteBuffer.allocate(66, false);
        buf.put(owner.type);
        buf.putInt(owner.id);
        buf.putInt(owner.instanceId);
        buf.put(source.type);
        buf.putInt(source.id);
        buf.putInt(source.instanceId);
        buf.putInt(level);
        buf.putLong(Time.currentTimeMillis(endTime));
        buf.putInt(multiple);
        buf.putInt(remainSeconds);
        buf.putInt(tickInterval);
        buf.putInt(remainCure);
        buf.putInt(remainDamage);
        buf.putInt(remainMPCure);
        buf.putInt(remainMPDamage);
        buf.putInt(remainAbsorb);
        buf.putInt(effectTimes);
        return buf.array();
    }

    public boolean update2(int time) {
        if (remainSeconds <= 0) {
            return false;
        }
        endTime += time;
        if (endTime <= Time.currTime) {
            return false;
        }
        return true;
    }

    public void enhance(PropertyCalculator pc) {
        if (!checkWeapon(pc.unit)) {
            return;
        }
        if (change_speed_percent > 0.0f) {
            pc.fast(multiple * change_speed_percent / 100.0f);
        } else {
            pc.slow(multiple * change_speed_percent / -100.0f);
        }
    }

    public void preHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (isActive) {
            context.threatAdd += multiple * change_threat_value;
            context.threatAddRate += multiple * change_threat_percent / 100.0f;
            effectTimes++;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (!isActive && context.damageType == CombatContext.DAMAGE_PHYSICAL) {
            int value = multiple * reduce_physical_damage_value;
            value += context.damage * multiple * reduce_physical_damage_percent / 100.0f;
            if (value > context.damage) {
                value = context.damage;
            }
            if (context.damage > 0) {
                context.threat -= value * context.threat / context.damage;
                context.damage -= value;
            }
            effectTimes++;
        }
        if (!isActive && context.damageType == CombatContext.DAMAGE_MAGIC) {
            int value = multiple * reduce_magic_damage_value;
            value += context.damage * multiple * reduce_magic_damage_percent / 100.0f;
            if (value > context.damage) {
                value = context.damage;
            }
            if (context.damage > 0) {
                context.threat -= value * context.threat / context.damage;
                context.damage -= value;
            }
            effectTimes++;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

}
