package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_630 implements Buff, PropertyEnhancer, Updatable {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, 60000, 180000, 180000, 300000, 600000
    };
    public static int[] CHANGE_STA_VALUE = {
        0, 0, 0, 0, 0, 0
    };
    public static float[] CHANGE_STA_RATE = {
        0.0f, 10.0f, 10.0f, 15.0f, 15.0f, 15.0f
    };
    public static int[] CHANGE_AGI_VALUE = {
        0, 0, 0, 0, 0, 0
    };
    public static float[] CHANGE_AGI_RATE = {
        0.0f, 10.0f, 10.0f, 15.0f, 15.0f, 15.0f
    };
    public static int[] CHANGE_STR_VALUE = {
        0, 0, 0, 0, 0, 0
    };
    public static float[] CHANGE_STR_RATE = {
        0.0f, 10.0f, 10.0f, 15.0f, 15.0f, 15.0f
    };
    public static int[] CHANGE_INT_VALUE = {
        0, 0, 0, 0, 0, 0
    };
    public static float[] CHANGE_INT_RATE = {
        0.0f, 10.0f, 10.0f, 15.0f, 15.0f, 15.0f
    };
    public static String[] LIMIT_MAP = {
        "", "2064,2065,2066,2067", "2064,2065,2066,2067", "2064,2065,2066,2067", "2064,2065,2066,2067", "2064,2065,2066,2067"
    };
    int instanceID;
    int realWeight;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    IntHashSet limitMaps = new IntHashSet();
    int change_sta_value;
    float change_sta_rate;
    int change_agi_value;
    float change_agi_rate;
    int change_str_value;
    float change_str_rate;
    int change_int_value;
    float change_int_rate;
    String limit_map;

    public AutoGeneratedBuff_630(int lvl, Unit src, Unit tgt, int dmg) {
        if (src != null) {
            source = src.ref();
        }
        if (tgt != null) {
            owner = tgt.ref();
        }
        instanceID = BuffUtil.getNextID();
        level = lvl;
        endTime = Time.currTime + BuffUtil.enhanceParam(src, tgt, 630, "buffTime", DURATION[level]);
        multiple = 1;
        change_sta_value = CHANGE_STA_VALUE[level];
        change_sta_value = BuffUtil.enhanceParam(src, tgt, 630, "change_sta_value", change_sta_value);
        change_sta_rate = CHANGE_STA_RATE[level];
        change_sta_rate = BuffUtil.enhanceParam(src, tgt, 630, "change_sta_rate", change_sta_rate);
        change_agi_value = CHANGE_AGI_VALUE[level];
        change_agi_value = BuffUtil.enhanceParam(src, tgt, 630, "change_agi_value", change_agi_value);
        change_agi_rate = CHANGE_AGI_RATE[level];
        change_agi_rate = BuffUtil.enhanceParam(src, tgt, 630, "change_agi_rate", change_agi_rate);
        change_str_value = CHANGE_STR_VALUE[level];
        change_str_value = BuffUtil.enhanceParam(src, tgt, 630, "change_str_value", change_str_value);
        change_str_rate = CHANGE_STR_RATE[level];
        change_str_rate = BuffUtil.enhanceParam(src, tgt, 630, "change_str_rate", change_str_rate);
        change_int_value = CHANGE_INT_VALUE[level];
        change_int_value = BuffUtil.enhanceParam(src, tgt, 630, "change_int_value", change_int_value);
        change_int_rate = CHANGE_INT_RATE[level];
        change_int_rate = BuffUtil.enhanceParam(src, tgt, 630, "change_int_rate", change_int_rate);
        limit_map = LIMIT_MAP[level];
        String[] secs = limit_map.split(",");
        for (String s : secs) {
            limitMaps.add(Integer.parseInt(s));
        }
    }

    public int getId() {
        return 630;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "荣誉塔扛把子";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("使用VIP荣誉塔药水后，在荣誉塔中打怪力量、敏捷、智力和体力均提升{0}。", CommonUtil.formatPercent(change_sta_rate * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return endTime;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return true;
    }

    public int getIconID() {
        return 38 | 0x1000000;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public int getMergeStrategy() {
        return 2;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public float getchange_dembuff_rate() {
         return 0;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_630) {
            AutoGeneratedBuff_630 other = (AutoGeneratedBuff_630)buff;
            if (level <= other.level) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure = other.remainCure;
                remainDamage = other.remainDamage;
                remainMPCure = other.remainMPCure;
                remainMPDamage = other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                effectTimes = 0;
                owner = other.owner;
                source = other.source;
                change_sta_value = other.change_sta_value;
                change_sta_rate = other.change_sta_rate;
                change_agi_value = other.change_agi_value;
                change_agi_rate = other.change_agi_rate;
                change_str_value = other.change_str_value;
                change_str_rate = other.change_str_rate;
                change_int_value = other.change_int_value;
                change_int_rate = other.change_int_rate;
                limit_map = other.limit_map;
            }
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
    }

    public boolean update(int diff) {
        if (endTime <= Time.currTime) {
            return true;
        }
        return false;
    }

    public void load(byte[] bytes) {
        ByteBuffer buf = ByteBuffer.wrap(bytes);
        owner = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        source = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        level = buf.getInt();
        endTime = Time.elapseTime(buf.getLong());
        multiple = buf.getInt();
        change_sta_value = CHANGE_STA_VALUE[level];
        change_sta_rate = CHANGE_STA_RATE[level];
        change_agi_value = CHANGE_AGI_VALUE[level];
        change_agi_rate = CHANGE_AGI_RATE[level];
        change_str_value = CHANGE_STR_VALUE[level];
        change_str_rate = CHANGE_STR_RATE[level];
        change_int_value = CHANGE_INT_VALUE[level];
        change_int_rate = CHANGE_INT_RATE[level];
        limit_map = LIMIT_MAP[level];
        remainSeconds = buf.getInt();
        tickInterval = buf.getInt();
        remainCure = buf.getInt();
        remainDamage = buf.getInt();
        remainMPCure = buf.getInt();
        remainMPDamage = buf.getInt();
        remainAbsorb = buf.getInt();
        effectTimes = buf.getInt();
    }

    public byte[] save() {
        ByteBuffer buf = ByteBuffer.allocate(66, false);
        buf.put(owner.type);
        buf.putInt(owner.id);
        buf.putInt(owner.instanceId);
        buf.put(source.type);
        buf.putInt(source.id);
        buf.putInt(source.instanceId);
        buf.putInt(level);
        buf.putLong(Time.currentTimeMillis(endTime));
        buf.putInt(multiple);
        buf.putInt(remainSeconds);
        buf.putInt(tickInterval);
        buf.putInt(remainCure);
        buf.putInt(remainDamage);
        buf.putInt(remainMPCure);
        buf.putInt(remainMPDamage);
        buf.putInt(remainAbsorb);
        buf.putInt(effectTimes);
        return buf.array();
    }

    public boolean update2(int time) {
        endTime += time;
        if (endTime <= Time.currTime) {
            return false;
        }
        return true;
    }

    public void enhance(PropertyCalculator pc) {
        if (!checkWeapon(pc.unit)) {
            return;
        }
        if (owner != null) {
            GameObject ownerUnit = ObjectAccessor.getGameObject(owner);
            if (ownerUnit != null && !limitMaps.contains(ownerUnit.map.id)) {
                return;
            }
        }
        pc.stamina *= 1.0f + multiple * change_sta_rate / 100.0f;
        pc.stamina += multiple * change_sta_value;
        pc.agility *= 1.0f + multiple * change_agi_rate / 100.0f;
        pc.agility += multiple * change_agi_value;
        pc.strength *= 1.0f + multiple * change_str_rate / 100.0f;
        pc.strength += multiple * change_str_value;
        pc.intellect *= 1.0f + multiple * change_int_rate / 100.0f;
        pc.intellect += multiple * change_int_value;
    }

}
