package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_542 implements Buff, PropertyEnhancer, Updatable {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, 10
    };
    public static int[] CHANGE_PHYSICAL_AP_VALUE = {
        0, 0
    };
    public static float[] CHANGE_PHYSICAL_AP_PERCENT = {
        0.0f, 50.0f
    };
    public static int[] CHANGE_MAGIC_AP_VALUE = {
        0, 0
    };
    public static float[] CHANGE_MAGIC_AP_PERCENT = {
        0.0f, 50.0f
    };
    public static int[] CHANGE_ARMOR_VALUE = {
        0, 0
    };
    public static float[] CHANGE_ARMOR_PERCENT = {
        0.0f, -100.0f
    };
    public static int[] CHANGE_MAGIC_ARMOR_VALUE = {
        0, 0
    };
    public static float[] CHANGE_MAGIC_ARMOR_PERCENT = {
        0.0f, -100.0f
    };
    public static float[] CHANGE_PHYSICAL_CRIT = {
        0.0f, 50.0f
    };
    public static float[] CHANGE_MAGIC_CRIT = {
        0.0f, 50.0f
    };
    public static int[] DOT_SECONDS = {
        0, 10
    };
    public static int[] DOT_TICK_TIME = {
        0, 2
    };
    public static int[] DOT_VALUE = {
        0, 15000
    };
    public static float[] DOT_PERCENT_OF_DAMAGE = {
        0.0f, 0.0f
    };
    int instanceID;
    int realWeight;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    int change_physical_ap_value;
    float change_physical_ap_percent;
    int change_magic_ap_value;
    float change_magic_ap_percent;
    int change_armor_value;
    float change_armor_percent;
    int change_magic_armor_value;
    float change_magic_armor_percent;
    float change_physical_crit;
    float change_magic_crit;
    int dot_seconds;
    int dot_tick_time;
    int dot_value;
    float dot_percent_of_damage;

    public AutoGeneratedBuff_542(int lvl, Unit src, Unit tgt, int dmg) {
        if (src != null) {
            source = src.ref();
        }
        if (tgt != null) {
            owner = tgt.ref();
        }
        instanceID = BuffUtil.getNextID();
        level = lvl;
        endTime = Time.currTime + BuffUtil.enhanceParam(src, tgt, 542, "buffTime", DURATION[level]);
        multiple = 1;
        change_physical_ap_value = CHANGE_PHYSICAL_AP_VALUE[level];
        change_physical_ap_value = BuffUtil.enhanceParam(src, tgt, 542, "change_physical_ap_value", change_physical_ap_value);
        change_physical_ap_percent = CHANGE_PHYSICAL_AP_PERCENT[level];
        change_physical_ap_percent = BuffUtil.enhanceParam(src, tgt, 542, "change_physical_ap_percent", change_physical_ap_percent);
        change_magic_ap_value = CHANGE_MAGIC_AP_VALUE[level];
        change_magic_ap_value = BuffUtil.enhanceParam(src, tgt, 542, "change_magic_ap_value", change_magic_ap_value);
        change_magic_ap_percent = CHANGE_MAGIC_AP_PERCENT[level];
        change_magic_ap_percent = BuffUtil.enhanceParam(src, tgt, 542, "change_magic_ap_percent", change_magic_ap_percent);
        change_armor_value = CHANGE_ARMOR_VALUE[level];
        change_armor_value = BuffUtil.enhanceParam(src, tgt, 542, "change_armor_value", change_armor_value);
        change_armor_percent = CHANGE_ARMOR_PERCENT[level];
        change_armor_percent = BuffUtil.enhanceParam(src, tgt, 542, "change_armor_percent", change_armor_percent);
        change_magic_armor_value = CHANGE_MAGIC_ARMOR_VALUE[level];
        change_magic_armor_value = BuffUtil.enhanceParam(src, tgt, 542, "change_magic_armor_value", change_magic_armor_value);
        change_magic_armor_percent = CHANGE_MAGIC_ARMOR_PERCENT[level];
        change_magic_armor_percent = BuffUtil.enhanceParam(src, tgt, 542, "change_magic_armor_percent", change_magic_armor_percent);
        change_physical_crit = CHANGE_PHYSICAL_CRIT[level];
        change_physical_crit = BuffUtil.enhanceParam(src, tgt, 542, "change_physical_crit", change_physical_crit);
        change_magic_crit = CHANGE_MAGIC_CRIT[level];
        change_magic_crit = BuffUtil.enhanceParam(src, tgt, 542, "change_magic_crit", change_magic_crit);
        dot_seconds = DOT_SECONDS[level];
        dot_seconds = BuffUtil.enhanceParam(src, tgt, 542, "dot_seconds", dot_seconds);
        dot_tick_time = DOT_TICK_TIME[level];
        dot_tick_time = BuffUtil.enhanceParam(src, tgt, 542, "dot_tick_time", dot_tick_time);
        dot_value = DOT_VALUE[level];
        dot_value = BuffUtil.enhanceParam(src, tgt, 542, "dot_value", dot_value);
        dot_percent_of_damage = DOT_PERCENT_OF_DAMAGE[level];
        dot_percent_of_damage = BuffUtil.enhanceParam(src, tgt, 542, "dot_percent_of_damage", dot_percent_of_damage);
        remainSeconds = dot_seconds;
        tickInterval = dot_tick_time;
        remainDamage = dot_value;
        remainDamage += dot_percent_of_damage * dmg / 100.0f;
    }

    public int getId() {
        return 542;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "中火";
    }

    public String getDesc() {
        return "10秒内造成15000伤害，降低100%护甲法防；同时提高50%伤害，提高50%暴击";
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return Time.currTime + remainSeconds * 1000;
    }

    public boolean isGood() {
        return false;
    }

    public boolean dispelable() {
        return true;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return 26 | 0x1000000;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public int getMergeStrategy() {
        return 4;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public float getchange_dembuff_rate() {
         return 0;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_542) {
            AutoGeneratedBuff_542 other = (AutoGeneratedBuff_542)buff;
            level = other.level;
            endTime = other.endTime;
            multiple = 1;
            remainSeconds = other.remainSeconds;
            tickInterval = other.tickInterval;
            remainCure = other.remainCure;
            remainDamage = other.remainDamage;
            remainMPCure = other.remainMPCure;
            remainMPDamage = other.remainMPDamage;
            remainAbsorb = other.remainAbsorb;
            effectTimes = 0;
            owner = other.owner;
            source = other.source;
            change_physical_ap_value = other.change_physical_ap_value;
            change_physical_ap_percent = other.change_physical_ap_percent;
            change_magic_ap_value = other.change_magic_ap_value;
            change_magic_ap_percent = other.change_magic_ap_percent;
            change_armor_value = other.change_armor_value;
            change_armor_percent = other.change_armor_percent;
            change_magic_armor_value = other.change_magic_armor_value;
            change_magic_armor_percent = other.change_magic_armor_percent;
            change_physical_crit = other.change_physical_crit;
            change_magic_crit = other.change_magic_crit;
            dot_seconds = other.dot_seconds;
            dot_tick_time = other.dot_tick_time;
            dot_value = other.dot_value;
            dot_percent_of_damage = other.dot_percent_of_damage;
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
    }

    public boolean update(int diff) {
        if (((remainSeconds - 1) % tickInterval) == 0) {
            int remainTicks = (remainSeconds - 1) / tickInterval + 1;
            int cure = remainCure / remainTicks;
            int dmg = remainDamage / remainTicks;
            int mpcure = remainMPCure / remainTicks;
            int mpdmg = remainMPDamage / remainTicks;
            remainCure -= cure;
            remainDamage -= dmg;
            remainMPCure -= mpcure;
            remainMPDamage -= mpdmg;
            if (cure > 0) {
                Unit healerUnit = (Unit)ObjectAccessor.getGameObject(source);
                Unit targetUnit = (Unit)ObjectAccessor.getGameObject(owner);
                if (targetUnit != null && targetUnit.isAlive()) {
                    cure = targetUnit.setHp(targetUnit.hp + cure, true);
                    if (healerUnit != null && healerUnit.isAlive()) {
                        Attack.addHealThreat(healerUnit, targetUnit, CombatContext.calcHotThreat(cure), false);
                    }
                }
            }
            if (dmg > 0) {
                Unit sourceUnit = (Unit)ObjectAccessor.getGameObject(source);
                Unit targetUnit = (Unit)ObjectAccessor.getGameObject(owner);
                if (targetUnit != null && targetUnit.isAlive()) {
                    targetUnit.setHp(targetUnit.hp - dmg, true);
                    if (sourceUnit != null && sourceUnit.isAlive()) {
                        Attack.addDamageThreat(sourceUnit, targetUnit, dmg, false);
                    }
                    if (targetUnit.hp <= 0) {
                        targetUnit.die(sourceUnit);
                    }
                }
            }
            if (mpcure > 0) {
                Unit healerUnit = (Unit)ObjectAccessor.getGameObject(source);
                Unit targetUnit = (Unit)ObjectAccessor.getGameObject(owner);
                if (targetUnit != null && targetUnit.isAlive()) {
                    targetUnit.setMp(targetUnit.mp + mpcure, true);
                    if (healerUnit != null && healerUnit.isAlive()) {
                        Attack.addHealThreat(healerUnit, targetUnit, mpcure, false);
                    }
                }
            }
            if (mpdmg > 0) {
                Unit sourceUnit = (Unit)ObjectAccessor.getGameObject(source);
                Unit targetUnit = (Unit)ObjectAccessor.getGameObject(owner);
                if (targetUnit != null && targetUnit.isAlive()) {
                    targetUnit.setMp(targetUnit.mp - mpdmg, true);
                    if (sourceUnit != null && sourceUnit.isAlive()) {
                        Attack.addDamageThreat(sourceUnit, targetUnit, mpdmg, false);
                    }
                }
            }
        }
        remainSeconds--;
        if (remainSeconds <= 0) {
            return true;
        }
        if (endTime <= Time.currTime) {
            return true;
        }
        return false;
    }

    public void load(byte[] bytes) {
        ByteBuffer buf = ByteBuffer.wrap(bytes);
        owner = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        source = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        level = buf.getInt();
        endTime = Time.elapseTime(buf.getLong());
        multiple = buf.getInt();
        change_physical_ap_value = CHANGE_PHYSICAL_AP_VALUE[level];
        change_physical_ap_percent = CHANGE_PHYSICAL_AP_PERCENT[level];
        change_magic_ap_value = CHANGE_MAGIC_AP_VALUE[level];
        change_magic_ap_percent = CHANGE_MAGIC_AP_PERCENT[level];
        change_armor_value = CHANGE_ARMOR_VALUE[level];
        change_armor_percent = CHANGE_ARMOR_PERCENT[level];
        change_magic_armor_value = CHANGE_MAGIC_ARMOR_VALUE[level];
        change_magic_armor_percent = CHANGE_MAGIC_ARMOR_PERCENT[level];
        change_physical_crit = CHANGE_PHYSICAL_CRIT[level];
        change_magic_crit = CHANGE_MAGIC_CRIT[level];
        dot_seconds = DOT_SECONDS[level];
        dot_tick_time = DOT_TICK_TIME[level];
        dot_value = DOT_VALUE[level];
        dot_percent_of_damage = DOT_PERCENT_OF_DAMAGE[level];
        remainSeconds = buf.getInt();
        tickInterval = buf.getInt();
        remainCure = buf.getInt();
        remainDamage = buf.getInt();
        remainMPCure = buf.getInt();
        remainMPDamage = buf.getInt();
        remainAbsorb = buf.getInt();
        effectTimes = buf.getInt();
    }

    public byte[] save() {
        ByteBuffer buf = ByteBuffer.allocate(66, false);
        buf.put(owner.type);
        buf.putInt(owner.id);
        buf.putInt(owner.instanceId);
        buf.put(source.type);
        buf.putInt(source.id);
        buf.putInt(source.instanceId);
        buf.putInt(level);
        buf.putLong(Time.currentTimeMillis(endTime));
        buf.putInt(multiple);
        buf.putInt(remainSeconds);
        buf.putInt(tickInterval);
        buf.putInt(remainCure);
        buf.putInt(remainDamage);
        buf.putInt(remainMPCure);
        buf.putInt(remainMPDamage);
        buf.putInt(remainAbsorb);
        buf.putInt(effectTimes);
        return buf.array();
    }

    public boolean update2(int time) {
        if (remainSeconds <= 0) {
            return false;
        }
        endTime += time;
        if (endTime <= Time.currTime) {
            return false;
        }
        return true;
    }

    public void enhance(PropertyCalculator pc) {
        if (!checkWeapon(pc.unit)) {
            return;
        }
        pc.attackpowerup += multiple * change_physical_ap_value;
        pc.attackpowerdown += multiple * change_physical_ap_value;
        pc.attackpowerRate += multiple * change_physical_ap_percent / 100.0f;
        pc.spellpowerRate += multiple * change_magic_ap_percent / 100.0f;
        pc.spellpower += multiple * change_magic_ap_value;
        pc.defenseRate += multiple * change_armor_percent / 100.0f;
        pc.defense += multiple * change_armor_value;
        if (pc.defense < 0) {
            pc.defense = 0;
        }
        pc.spellDefenseRate += multiple * change_magic_armor_percent / 100.0f;
        pc.spelldefense += multiple * change_magic_armor_value;
        if (pc.spelldefense < 0) {
            pc.spelldefense = 0;
        }
        pc.critical += multiple * change_physical_crit / 100.0f;
        pc.spellcritical += multiple * change_magic_crit / 100.0f;
    }

}
