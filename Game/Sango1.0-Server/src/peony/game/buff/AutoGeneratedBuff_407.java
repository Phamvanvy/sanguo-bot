package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_407 implements Buff, Updatable {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, -1, -1, -1, -1, -1
    };
    public static float[] DUMB_BUFF_RATE = {
        0.0f, 100.0f, 100.0f, 100.0f, 100.0f, 100.0f
    };
    public static String[] DUMB_BUFF_RATE_VAR = {
        "", "", "", "", "", ""
    };
    public static int[] DUMB_BUFF_ID = {
        0, 408, 408, 408, 408, 408
    };
    public static int[] DUMB_BUFF_LEVEL = {
        0, 1, 2, 3, 4, 5
    };
    int instanceID;
    int realWeight;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    boolean dumbBuffActive;
    float dumb_buff_rate;
    String dumb_buff_rate_var;
    int dumb_buff_id;
    int dumb_buff_level;


    public AutoGeneratedBuff_407(int lvl) {
        int dmg = 0;
        instanceID = BuffUtil.getNextID();
        level = lvl;
        multiple = 1;
        dumb_buff_rate = DUMB_BUFF_RATE[level];
        dumb_buff_rate_var = DUMB_BUFF_RATE_VAR[level];
        dumb_buff_id = DUMB_BUFF_ID[level];
        dumb_buff_level = DUMB_BUFF_LEVEL[level];
    }

    public int getId() {
        return 407;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "马套装BUFF";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("被沉默或者禁咒后有{0}几率使自己在{1}内护甲和法防提高{2}", CommonUtil.formatPercent(dumb_buff_rate * multiple), CommonUtil.formatMillSecond(peony.game.buff.AutoGeneratedBuff_408.DURATION[dumb_buff_level]), CommonUtil.formatPercent(peony.game.buff.AutoGeneratedBuff_408.CHANGE_ARMOR_PERCENT[dumb_buff_level] * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return -1;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return -1;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public int getMergeStrategy() {
        return 2;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public float getchange_dembuff_rate() {
         return 0;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_407) {
            AutoGeneratedBuff_407 other = (AutoGeneratedBuff_407)buff;
            if (level <= other.level) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure = other.remainCure;
                remainDamage = other.remainDamage;
                remainMPCure = other.remainMPCure;
                remainMPDamage = other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                effectTimes = 0;
                owner = other.owner;
                source = other.source;
                dumb_buff_rate = other.dumb_buff_rate;
                dumb_buff_rate_var = other.dumb_buff_rate_var;
                dumb_buff_id = other.dumb_buff_id;
                dumb_buff_level = other.dumb_buff_level;
            }
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
        dumb_buff_rate = DUMB_BUFF_RATE[level];
        dumb_buff_rate = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 407, "dumb_buff_rate", dumb_buff_rate);
        dumb_buff_level = DUMB_BUFF_LEVEL[level];
        dumb_buff_level = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 407, "dumb_buff_level", dumb_buff_level);
    }

    public boolean update(int diff) {
        Unit targetUnit = (Unit)ObjectAccessor.getGameObject(owner);
        if (targetUnit == null) {
            return true;
        }
        boolean hit = CommonUtil.hit(RND, (int)dumb_buff_rate, 100);
        if (dumbBuffActive && !targetUnit.isDumb()) {
            dumbBuffActive = false;
        } else if (!dumbBuffActive && targetUnit.isDumb() && hit) {
            targetUnit.buffs.addBuff(BuffUtil.createBuff((int)dumb_buff_id, dumb_buff_level, targetUnit, targetUnit, 0));
            dumbBuffActive = true;
        }
        return false;
    }

    public void load(byte[] bytes) {
        ByteBuffer buf = ByteBuffer.wrap(bytes);
        owner = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        source = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        level = buf.getInt();
        endTime = Time.elapseTime(buf.getLong());
        multiple = buf.getInt();
        dumb_buff_rate = DUMB_BUFF_RATE[level];
        dumb_buff_rate_var = DUMB_BUFF_RATE_VAR[level];
        dumb_buff_id = DUMB_BUFF_ID[level];
        dumb_buff_level = DUMB_BUFF_LEVEL[level];
        remainSeconds = buf.getInt();
        tickInterval = buf.getInt();
        remainCure = buf.getInt();
        remainDamage = buf.getInt();
        remainMPCure = buf.getInt();
        remainMPDamage = buf.getInt();
        remainAbsorb = buf.getInt();
        effectTimes = buf.getInt();
    }

    public byte[] save() {
        ByteBuffer buf = ByteBuffer.allocate(66, false);
        buf.put(owner.type);
        buf.putInt(owner.id);
        buf.putInt(owner.instanceId);
        buf.put(source.type);
        buf.putInt(source.id);
        buf.putInt(source.instanceId);
        buf.putInt(level);
        buf.putLong(Time.currentTimeMillis(endTime));
        buf.putInt(multiple);
        buf.putInt(remainSeconds);
        buf.putInt(tickInterval);
        buf.putInt(remainCure);
        buf.putInt(remainDamage);
        buf.putInt(remainMPCure);
        buf.putInt(remainMPDamage);
        buf.putInt(remainAbsorb);
        buf.putInt(effectTimes);
        return buf.array();
    }

    public boolean update2(int time) {
        return true;
    }

}
