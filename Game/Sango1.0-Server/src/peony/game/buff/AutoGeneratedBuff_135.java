package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_135 implements Buff, PropertyEnhancer, Updatable {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, 3600000, 3600000
    };
    public static float[] CHANGE_EXP_RATE = {
        0.0f, 100.0f, 200.0f
    };
    public static float[] CHANGE_PEOPLEEXP_RATE_BY_TONG_SKILL = {
        0.0f, 0.0f, 0.0f
    };
    int instanceID;
    int realWeight;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    float change_exp_rate;
    float change_peopleexp_rate_by_tong_skill;

    public AutoGeneratedBuff_135(int lvl, Unit src, Unit tgt, int dmg) {
        if (src != null) {
            source = src.ref();
        }
        if (tgt != null) {
            owner = tgt.ref();
        }
        instanceID = BuffUtil.getNextID();
        level = lvl;
        endTime = Time.currTime + BuffUtil.enhanceParam(src, tgt, 135, "buffTime", DURATION[level]);
        multiple = 1;
        change_exp_rate = CHANGE_EXP_RATE[level];
        change_exp_rate = BuffUtil.enhanceParam(src, tgt, 135, "change_exp_rate", change_exp_rate);
        change_peopleexp_rate_by_tong_skill = CHANGE_PEOPLEEXP_RATE_BY_TONG_SKILL[level];
        change_peopleexp_rate_by_tong_skill = BuffUtil.enhanceParam(src, tgt, 135, "change_peopleexp_rate_by_tong_skill", change_peopleexp_rate_by_tong_skill);
    }

    public int getId() {
        return 135;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "多倍经验";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("杀死怪物获得经验值提高{0}", CommonUtil.formatPercent(change_exp_rate * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return endTime;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return true;
    }

    public int getIconID() {
        return 74 | 0x1000000;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public int getMergeStrategy() {
        return 2;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public float getchange_dembuff_rate() {
         return 0;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_135) {
            AutoGeneratedBuff_135 other = (AutoGeneratedBuff_135)buff;
            if (level <= other.level) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure = other.remainCure;
                remainDamage = other.remainDamage;
                remainMPCure = other.remainMPCure;
                remainMPDamage = other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                effectTimes = 0;
                owner = other.owner;
                source = other.source;
                change_exp_rate = other.change_exp_rate;
                change_peopleexp_rate_by_tong_skill = other.change_peopleexp_rate_by_tong_skill;
            }
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
    }

    public boolean update(int diff) {
        if (endTime <= Time.currTime) {
            return true;
        }
        return false;
    }

    public void load(byte[] bytes) {
        ByteBuffer buf = ByteBuffer.wrap(bytes);
        owner = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        source = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        level = buf.getInt();
        endTime = Time.elapseTime(buf.getLong());
        multiple = buf.getInt();
        change_exp_rate = CHANGE_EXP_RATE[level];
        change_peopleexp_rate_by_tong_skill = CHANGE_PEOPLEEXP_RATE_BY_TONG_SKILL[level];
        remainSeconds = buf.getInt();
        tickInterval = buf.getInt();
        remainCure = buf.getInt();
        remainDamage = buf.getInt();
        remainMPCure = buf.getInt();
        remainMPDamage = buf.getInt();
        remainAbsorb = buf.getInt();
        effectTimes = buf.getInt();
    }

    public byte[] save() {
        ByteBuffer buf = ByteBuffer.allocate(66, false);
        buf.put(owner.type);
        buf.putInt(owner.id);
        buf.putInt(owner.instanceId);
        buf.put(source.type);
        buf.putInt(source.id);
        buf.putInt(source.instanceId);
        buf.putInt(level);
        buf.putLong(Time.currentTimeMillis(endTime));
        buf.putInt(multiple);
        buf.putInt(remainSeconds);
        buf.putInt(tickInterval);
        buf.putInt(remainCure);
        buf.putInt(remainDamage);
        buf.putInt(remainMPCure);
        buf.putInt(remainMPDamage);
        buf.putInt(remainAbsorb);
        buf.putInt(effectTimes);
        return buf.array();
    }

    public boolean update2(int time) {
        endTime += time;
        if (endTime <= Time.currTime) {
            return false;
        }
        return true;
    }

    public void enhance(PropertyCalculator pc) {
        if (!checkWeapon(pc.unit)) {
            return;
        }
        pc.expRatio += multiple * change_exp_rate / 100.0f;
        peony.service.tong.TongService ts = Server.server.getServiceRegistry().getTongService();
        peony.service.tong.TongMember tm = ts.getPlayerInfo(pc.unit.id);
        if(tm!=null && tm.skills!=null && tm.skills.get(9)!=null){
           peony.service.tong.TongSkill9 tskill = (peony.service.tong.TongSkill9)tm.skills.get(9);
           int ration = tskill.getRatios();
           pc.expRatio += ration / 100.0f;
         }
    }

}
