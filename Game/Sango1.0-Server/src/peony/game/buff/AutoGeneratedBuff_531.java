package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_531 implements Buff, CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, -1
    };
    public static int[] CHANGE_B_PHYSICAL_AP_VALUE = {
        0, 0
    };
    public static float[] CHANGE_B_PHYSICAL_AP_PERCENT = {
        0.0f, 100.0f
    };
    public static int[] CHANGE_B_MAGIC_AP_VALUE = {
        0, 0
    };
    public static float[] CHANGE_B_MAGIC_AP_PERCENT = {
        0.0f, 100.0f
    };
    int instanceID;
    int realWeight;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    int change_b_physical_ap_value;
    float change_b_physical_ap_percent;
    int change_b_magic_ap_value;
    float change_b_magic_ap_percent;

    public AutoGeneratedBuff_531(int lvl, Unit src, Unit tgt, int dmg) {
        if (src != null) {
            source = src.ref();
        }
        if (tgt != null) {
            owner = tgt.ref();
        }
        instanceID = BuffUtil.getNextID();
        level = lvl;
        endTime = Time.currTime + BuffUtil.enhanceParam(src, tgt, 531, "buffTime", DURATION[level]);
        multiple = 1;
        change_b_physical_ap_value = CHANGE_B_PHYSICAL_AP_VALUE[level];
        change_b_physical_ap_value = BuffUtil.enhanceParam(src, tgt, 531, "change_b_physical_ap_value", change_b_physical_ap_value);
        change_b_physical_ap_percent = CHANGE_B_PHYSICAL_AP_PERCENT[level];
        change_b_physical_ap_percent = BuffUtil.enhanceParam(src, tgt, 531, "change_b_physical_ap_percent", change_b_physical_ap_percent);
        change_b_magic_ap_value = CHANGE_B_MAGIC_AP_VALUE[level];
        change_b_magic_ap_value = BuffUtil.enhanceParam(src, tgt, 531, "change_b_magic_ap_value", change_b_magic_ap_value);
        change_b_magic_ap_percent = CHANGE_B_MAGIC_AP_PERCENT[level];
        change_b_magic_ap_percent = BuffUtil.enhanceParam(src, tgt, 531, "change_b_magic_ap_percent", change_b_magic_ap_percent);
    }

    public int getId() {
        return 531;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "火属性增强效果";
    }

    public String getDesc() {
        return "用此效果去攻击金属性BOSS时，物理和法术攻击力提升100%";
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return -1;
    }

    public boolean isGood() {
        return false;
    }

    public boolean dispelable() {
        return true;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return 74 | 0x1000000;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public int getMergeStrategy() {
        return 2;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public float getchange_dembuff_rate() {
         return 0;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_531) {
            AutoGeneratedBuff_531 other = (AutoGeneratedBuff_531)buff;
            if (level <= other.level) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure = other.remainCure;
                remainDamage = other.remainDamage;
                remainMPCure = other.remainMPCure;
                remainMPDamage = other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                effectTimes = 0;
                owner = other.owner;
                source = other.source;
                change_b_physical_ap_value = other.change_b_physical_ap_value;
                change_b_physical_ap_percent = other.change_b_physical_ap_percent;
                change_b_magic_ap_value = other.change_b_magic_ap_value;
                change_b_magic_ap_percent = other.change_b_magic_ap_percent;
            }
            return true;
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
    }

    public void preHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (isActive && context.damageType == CombatContext.DAMAGE_PHYSICAL) {
            context.attackPower += change_b_physical_ap_value;
            context.attackPowerRate += change_b_physical_ap_percent / 100.0f;
            effectTimes++;
        }
        if (isActive && (context.damageType == CombatContext.DAMAGE_MAGIC || context.damageType == CombatContext.DAMAGE_DECMP)) {
            context.attackPower += change_b_magic_ap_value;
            context.attackPowerRate += change_b_magic_ap_percent / 100.0f;
            effectTimes++;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

}
