package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;
import peony.game.itemenhance.ItemEnhance;

public class AutoGeneratedBuff_596 implements Buff, PropertyEnhancer, Updatable, CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public int buffover_addbuff = 0;
    public int buffover_addbuff_level = 0;
    public static int[] DURATION = {
        0, 1800000
    };
    public static int[] CHANGE_PHYSICAL_AP_VALUE = {
        0, 900
    };
    public static float[] CHANGE_PHYSICAL_AP_PERCENT = {
        0.0f, 0.0f
    };
    public static int[] CHANGE_MAGIC_AP_VALUE = {
        0, 600
    };
    public static float[] CHANGE_MAGIC_AP_PERCENT = {
        0.0f, 0.0f
    };
    public static int[] CHANGE_PHYSICAL_DODGE_RATE = {
        0, 180
    };
    public static int[] CHANGE_MAGIC_DODGE_RATE = {
        0, 180
    };
    public static int[] CHANGE_CURE_VALUE = {
        0, 600
    };
    public static float[] CHANGE_CURE_PERCENT = {
        0.0f, 0.0f
    };
    public static int[] CHANGE_MAXHP_VALUE = {
        0, 5000
    };
    public static float[] CHANGE_MAXHP_PERCENT = {
        0.0f, 0.0f
    };
    int instanceID;
    int realWeight;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    int change_physical_ap_value;
    float change_physical_ap_percent;
    int change_magic_ap_value;
    float change_magic_ap_percent;
    int change_physical_dodge_rate;
    int change_magic_dodge_rate;
    int change_cure_value;
    float change_cure_percent;
    int change_maxhp_value;
    float change_maxhp_percent;

    public AutoGeneratedBuff_596(int lvl, Unit src, Unit tgt, int dmg) {
        if (src != null) {
            source = src.ref();
        }
        if (tgt != null) {
            owner = tgt.ref();
        }
        instanceID = BuffUtil.getNextID();
        level = lvl;
        endTime = Time.currTime + BuffUtil.enhanceParam(src, tgt, 596, "buffTime", DURATION[level]);
        multiple = 1;
        change_physical_ap_value = CHANGE_PHYSICAL_AP_VALUE[level];
        change_physical_ap_value = BuffUtil.enhanceParam(src, tgt, 596, "change_physical_ap_value", change_physical_ap_value);
        change_physical_ap_percent = CHANGE_PHYSICAL_AP_PERCENT[level];
        change_physical_ap_percent = BuffUtil.enhanceParam(src, tgt, 596, "change_physical_ap_percent", change_physical_ap_percent);
        change_magic_ap_value = CHANGE_MAGIC_AP_VALUE[level];
        change_magic_ap_value = BuffUtil.enhanceParam(src, tgt, 596, "change_magic_ap_value", change_magic_ap_value);
        change_magic_ap_percent = CHANGE_MAGIC_AP_PERCENT[level];
        change_magic_ap_percent = BuffUtil.enhanceParam(src, tgt, 596, "change_magic_ap_percent", change_magic_ap_percent);
        change_physical_dodge_rate = CHANGE_PHYSICAL_DODGE_RATE[level];
        change_physical_dodge_rate = BuffUtil.enhanceParam(src, tgt, 596, "change_physical_dodge_rate", change_physical_dodge_rate);
        change_magic_dodge_rate = CHANGE_MAGIC_DODGE_RATE[level];
        change_magic_dodge_rate = BuffUtil.enhanceParam(src, tgt, 596, "change_magic_dodge_rate", change_magic_dodge_rate);
        change_cure_value = CHANGE_CURE_VALUE[level];
        change_cure_value = BuffUtil.enhanceParam(src, tgt, 596, "change_cure_value", change_cure_value);
        change_cure_percent = CHANGE_CURE_PERCENT[level];
        change_cure_percent = BuffUtil.enhanceParam(src, tgt, 596, "change_cure_percent", change_cure_percent);
        change_maxhp_value = CHANGE_MAXHP_VALUE[level];
        change_maxhp_value = BuffUtil.enhanceParam(src, tgt, 596, "change_maxhp_value", change_maxhp_value);
        change_maxhp_percent = CHANGE_MAXHP_PERCENT[level];
        change_maxhp_percent = BuffUtil.enhanceParam(src, tgt, 596, "change_maxhp_percent", change_maxhp_percent);
    }

    public int getId() {
        return 596;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "十星特效buff";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("物理攻击提升{0}点；法术攻击提升{1}点；生命上限提升{2}点；物理和法术闪避提升{3}点；使用还魂香包复活角色时恢复100%的生命值和气力/精力值。", CommonUtil.formatValue(change_physical_ap_value * multiple), CommonUtil.formatValue(change_magic_ap_value * multiple), CommonUtil.formatValue(change_maxhp_value * multiple), CommonUtil.formatValue(change_physical_dodge_rate * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return endTime;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return true;
    }

    public int getIconID() {
        return 154 | 0x1000000;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public int getMergeStrategy() {
        return 3;
    }

    public boolean isNeedMerge() {
        return true;
    }

    public float getchange_dembuff_rate() {
         return 0;
    }

    public boolean merge(Buff buff) {
        if (buff instanceof AutoGeneratedBuff_596) {
            AutoGeneratedBuff_596 other = (AutoGeneratedBuff_596)buff;
            if (source.equals(other.source)) {
                level = other.level;
                endTime = other.endTime;
                multiple = 1;
                remainSeconds = other.remainSeconds;
                tickInterval = other.tickInterval;
                remainCure += other.remainCure;
                remainDamage += other.remainDamage;
                remainMPCure += other.remainMPCure;
                remainMPDamage += other.remainMPDamage;
                remainAbsorb = other.remainAbsorb;
                change_physical_ap_value = other.change_physical_ap_value;
                change_physical_ap_percent = other.change_physical_ap_percent;
                change_magic_ap_value = other.change_magic_ap_value;
                change_magic_ap_percent = other.change_magic_ap_percent;
                change_physical_dodge_rate = other.change_physical_dodge_rate;
                change_magic_dodge_rate = other.change_magic_dodge_rate;
                change_cure_value = other.change_cure_value;
                change_cure_percent = other.change_cure_percent;
                change_maxhp_value = other.change_maxhp_value;
                change_maxhp_percent = other.change_maxhp_percent;
                return true;
            }
        }
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
    }

    public boolean update(int diff) {
        if (endTime <= Time.currTime) {
            return true;
        }
        return false;
    }

    public void load(byte[] bytes) {
        ByteBuffer buf = ByteBuffer.wrap(bytes);
        owner = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        source = new GameObjectRef(buf.get(), buf.getInt(), buf.getInt());
        level = buf.getInt();
        endTime = Time.elapseTime(buf.getLong());
        multiple = buf.getInt();
        change_physical_ap_value = CHANGE_PHYSICAL_AP_VALUE[level];
        change_physical_ap_percent = CHANGE_PHYSICAL_AP_PERCENT[level];
        change_magic_ap_value = CHANGE_MAGIC_AP_VALUE[level];
        change_magic_ap_percent = CHANGE_MAGIC_AP_PERCENT[level];
        change_physical_dodge_rate = CHANGE_PHYSICAL_DODGE_RATE[level];
        change_magic_dodge_rate = CHANGE_MAGIC_DODGE_RATE[level];
        change_cure_value = CHANGE_CURE_VALUE[level];
        change_cure_percent = CHANGE_CURE_PERCENT[level];
        change_maxhp_value = CHANGE_MAXHP_VALUE[level];
        change_maxhp_percent = CHANGE_MAXHP_PERCENT[level];
        remainSeconds = buf.getInt();
        tickInterval = buf.getInt();
        remainCure = buf.getInt();
        remainDamage = buf.getInt();
        remainMPCure = buf.getInt();
        remainMPDamage = buf.getInt();
        remainAbsorb = buf.getInt();
        effectTimes = buf.getInt();
    }

    public byte[] save() {
        ByteBuffer buf = ByteBuffer.allocate(66, false);
        buf.put(owner.type);
        buf.putInt(owner.id);
        buf.putInt(owner.instanceId);
        buf.put(source.type);
        buf.putInt(source.id);
        buf.putInt(source.instanceId);
        buf.putInt(level);
        buf.putLong(Time.currentTimeMillis(endTime));
        buf.putInt(multiple);
        buf.putInt(remainSeconds);
        buf.putInt(tickInterval);
        buf.putInt(remainCure);
        buf.putInt(remainDamage);
        buf.putInt(remainMPCure);
        buf.putInt(remainMPDamage);
        buf.putInt(remainAbsorb);
        buf.putInt(effectTimes);
        return buf.array();
    }

    public boolean update2(int time) {
        endTime += time;
        if (endTime <= Time.currTime) {
            return false;
        }
        return true;
    }

    public void enhance(PropertyCalculator pc) {
        if (!checkWeapon(pc.unit)) {
            return;
        }
        pc.attackpowerup += multiple * change_physical_ap_value;
        pc.attackpowerdown += multiple * change_physical_ap_value;
        pc.attackpowerRate += multiple * change_physical_ap_percent / 100.0f;
        pc.spellpowerRate += multiple * change_magic_ap_percent / 100.0f;
        pc.spellpower += multiple * change_magic_ap_value;
        pc.dodgerating += multiple * change_physical_dodge_rate;
        pc.spelldodgerating += multiple * change_magic_dodge_rate;
        pc.hp += multiple * change_maxhp_value;
        pc.hpRate += multiple * change_maxhp_percent / 100.0f;
    }

    public void preHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (isActive && context.damageType == CombatContext.DAMAGE_HEAL && !context.isAdditive) {
            context.damage += context.damage * multiple * change_cure_percent / 100.0f;
            context.damage += multiple * change_cure_value;
            effectTimes++;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

}
