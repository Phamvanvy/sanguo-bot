package peony.game.skill;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import peony.game.attendant.*;

public class AutoGeneratedSkill_420 extends AbstractSkill implements CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public static int[] REQUIRE_LEVEL = {
        0, 0
    };
    public static float[] MP_USE = {
        0.0f, 400.0f
    };
    public static int[] ACT_TIME = {
        0, 2000
    };
    public static int[] CD_TIME = {
        0, 1800000
    };
    public static int[] DISTANCE = {
        0, 120
    };
    public static int[] RANGE = {
        0, 0
    };
    public static float[] RELIVE_RESTORE_PERCENT = {
        0.0f, 30.0f
    };
    float relive_restore_percent;

    public AutoGeneratedSkill_420(int lvl) {
        super(420, "情深似海", lvl);
        if (lvl > 1) {
            throw new IllegalArgumentException();
        }
        relive_restore_percent = RELIVE_RESTORE_PERCENT[lvl];
        distance = DISTANCE[lvl];
        actTime = ACT_TIME[lvl];
        CDGroup = 420;
        CDTime = CD_TIME[lvl];
        range = RANGE[lvl];
        iconId = 45;
        prepareAnimation = -1;
        castAnimation = -1;
        hitAnimation = 114;
        clazz = 5;
        mp = MP_USE[lvl];
        type = TYPE_RELIVE;
        type |= TYPE_VISIBLE;
        type |= TYPE_RIDE_USE;
        targetType = TARGET_SINGLE_AID;
    }

    public String getDesc(Unit owner) {
        return java.text.MessageFormat.format("当恩爱度达到90点时方可激活，此技能可以复活你的配偶，并恢复配偶的{0}生命与精力", CommonUtil.formatPercent(relive_restore_percent));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getRequireLevel() {
        return REQUIRE_LEVEL[level];
    }

    protected CombatEffect createActEffect() {
        if (level == 0) {
            return null;
        }
        return this;
    }

    public Buff newBuff() {
        return null;
    }

    public Buff getAreaBuff() {
        return null;
    }

    public void preHit(CombatContext context, boolean isActive) {
        context.damageType = CombatContext.DAMAGE_HEAL;
        if (!checkWeapon(context.source)) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {}

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(context.source)) {
            return;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(context.source)) {
            return;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(context.source)) {
            return;
        }
        if (context.source != context.target) {
            if (isActive) {
                if (!context.target.isAlive()) {
                    // TODO: 向死亡的人发送一个复活请求
                    if (context.target.type == GameObject.TYPE_PLAYER&&((Player)context.source).getMateID()==context.target.id) {
                        ReliveOption option = new ReliveOption(
                                ReliveOption.SKILL_ACTIVE, getName(), 14,
                               context.target.map.id, context.source.x,
                               context.source.y);
                       option.context = context;
                      ((Player)context.target).addReliveOption(option);
                    }
                }
             } else {
                if (!context.target.isAlive()) {
                     int hp = (int)(context.target.maxhp * context.getSkillParam(this, "relive_restore_percent", relive_restore_percent) / 100.0f);
                     int mp = (int)(context.target.maxmp * context.getSkillParam(this, "relive_restore_percent", relive_restore_percent) / 100.0f);
                     context.target.relive(hp, mp);
                }
            }
         }
    }

}
