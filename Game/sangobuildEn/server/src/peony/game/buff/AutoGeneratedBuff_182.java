package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;

public class AutoGeneratedBuff_182 implements Buff, PropertyEnhancer {
    private static final int[] WEAPON = {
        
    };
    public static int[] DURATION = {
        0, -1, -1
    };
    public static int[] CHANGE_BASIC_MP_VALUE = {
        0, 400, 800
    };
    public static float[] CHANGE_BASIC_MP_RATE = {
        0.0f, 0.0f, 0.0f
    };
    int instanceID;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    int change_basic_mp_value;
    float change_basic_mp_rate;

    public AutoGeneratedBuff_182(int lvl) {
        int dmg = 0;
        instanceID = BuffUtil.getNextID();
        level = lvl;
        multiple = 1;
        change_basic_mp_value = CHANGE_BASIC_MP_VALUE[level];
        change_basic_mp_rate = CHANGE_BASIC_MP_RATE[level];
    }

    public int getId() {
        return 182;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "藍套加魔";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("精力/氣力值上限提升{0}點", CommonUtil.formatValue(change_basic_mp_value * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return -1;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return -1;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public boolean isNeedMerge() {
        return false;
    }

    public boolean merge(Buff buff) {
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
        change_basic_mp_value = CHANGE_BASIC_MP_VALUE[level];
        change_basic_mp_value = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 182, "change_basic_mp_value", change_basic_mp_value);
        change_basic_mp_rate = CHANGE_BASIC_MP_RATE[level];
        change_basic_mp_rate = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 182, "change_basic_mp_rate", change_basic_mp_rate);
    }

    public void enhance(PropertyCalculator pc) {
        if (!checkWeapon(pc.unit)) {
            return;
        }
        pc.mp += multiple * change_basic_mp_value;
        pc.basicMpRate += multiple * change_basic_mp_rate / 100.0f;
    }

}
