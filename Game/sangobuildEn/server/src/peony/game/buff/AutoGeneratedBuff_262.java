package peony.game.buff;

import java.util.*;
import peony.game.*;
import peony.game.buff.*;
import peony.game.skill.*;
import peony.util.*;
import com.pip.util.*;
import org.apache.mina.common.ByteBuffer;

public class AutoGeneratedBuff_262 implements Buff, CombatEffect {
    private static final int[] WEAPON = {
        
    };
    public static int[] DURATION = {
        0, -1, -1, -1, -1, -1
    };
    public static float[] CRIT_BUFF_RATE = {
        0.0f, 10.0f, 30.0f, 50.0f, 70.0f, 90.0f
    };
    public static int[] CRIT_BUFF_ID = {
        0, 263, 263, 263, 263, 263
    };
    public static int[] CRIT_BUFF_LEVEL = {
        0, 1, 2, 3, 4, 5
    };
    int instanceID;
    int level;
    int endTime;
    int multiple;
    int remainSeconds;
    int tickInterval;
    int remainCure;
    int remainDamage;
    int remainMPCure;
    int remainMPDamage;
    int remainAbsorb;
    int effectTimes;
    GameObjectRef owner;
    GameObjectRef source;
    float crit_buff_rate;
    int crit_buff_id;
    int crit_buff_level;

    public AutoGeneratedBuff_262(int lvl) {
        int dmg = 0;
        instanceID = BuffUtil.getNextID();
        level = lvl;
        multiple = 1;
        crit_buff_rate = CRIT_BUFF_RATE[level];
        crit_buff_id = CRIT_BUFF_ID[level];
        crit_buff_level = CRIT_BUFF_LEVEL[level];
    }

    public int getId() {
        return 262;
    }

    public int getInstanceID() {
        return instanceID;
    }

    public String getName() {
        return "暴擊勇往直前";
    }

    public String getDesc() {
        return java.text.MessageFormat.format("暴擊後有{0}幾率使自己5秒內攻擊無視目標護甲法防{1},同時自身護甲法防降低{2}", CommonUtil.formatPercent(crit_buff_rate * multiple), CommonUtil.formatPercent(peony.game.buff.AutoGeneratedBuff_263.IGNORE_ARMOR_PERCENT[crit_buff_level] * multiple), CommonUtil.formatPercent(peony.game.buff.AutoGeneratedBuff_263.CHANGE_ARMOR_PERCENT[crit_buff_level] * multiple));
    }

    private boolean checkWeapon(Unit unit) {
        return true;
    }

    public int getEndTime() {
        return -1;
    }

    public boolean isGood() {
        return true;
    }

    public boolean dispelable() {
        return false;
    }

    public boolean keepOnDie() {
        return false;
    }

    public int getIconID() {
        return -1;
    }

    public boolean isAreaBuff() {
        return false;
    }

    public boolean isNeedMerge() {
        return false;
    }

    public boolean merge(Buff buff) {
        return false;
    }

    public GameObjectRef getSource() {
        return source;
    }

    public void setOwner(GameObjectRef o) {
        owner = o;
        if (source == null) {
            source = owner;
        }
    }

    public void resetParams(Unit owner) {
        crit_buff_rate = CRIT_BUFF_RATE[level];
        crit_buff_rate = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 262, "crit_buff_rate", crit_buff_rate);
        crit_buff_level = CRIT_BUFF_LEVEL[level];
        crit_buff_level = owner.buffs.getParamEnhances().enhance(ParamEnhanceSet.TYPE_BUFF_OWNER, 262, "crit_buff_level", crit_buff_level);
    }

    public void preHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postHit(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void preDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void postDamage(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
    }

    public void finished(CombatContext context, boolean isActive) {
        if (!checkWeapon(isActive ? context.source : context.target)) {
            return;
        }
        if (isActive && context.critical() && context.isAttack()) {
            boolean hit = CommonUtil.hit(RND, (int)(multiple * 100 * crit_buff_rate), 10000);
            if (hit) {
                context.source.buffs.addBuff(BuffUtil.createBuff(crit_buff_id, crit_buff_level, context.source, context.source, context.damage));
                effectTimes++;
            }
        }
    }

}
